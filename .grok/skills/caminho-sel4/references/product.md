# Product guarantees (RFC-0191)

Source of truth: `docs/rfc/0191-pacote-garantias-produto.md`. This file is
the Fire recipe. Do not restate the four sentences here.

When ranks 1–10 are empty, **product remaining** wins over cartoon
(Montanha frozen) and over a random trampoline `if`. `candidates.py`
prints `unpaid_product` and `leftover_next product remaining RFC-0191 Pn.m`.

## Order (one Fire = one slice)

1. **P0.1** — `scripts/ratchet/product_guarantees.tsv` +
   `scripts/check_product_floor.py --selftest` + job `product-floor`.
   Freeze R1=`atom` (`visible_at_deletion_never_live`); D1/T1/C1=`model`.
   This **is** a land (same class as `depth-floor`). Flip the RFC checkbox
   in the same commit.
2. **P0.2** — product corollary: Lean `∀` that `unfold`s `merge.visible_at`,
   names R1, as-is dente. TSV R1 points at this theorem.
3. **P0.3** — ledger table. Then **P1.1–P1.4** (R1-value, D1 ∀ Bool,
   T1 leftover kernel, C1 ∀ counts). Then **P2.1 / P2.2** (one Inv lemma
   each). Then **P1.5 / P2.3** trampoline (`cap_data_fate` down + atom
   same commit).

## Credit (0188, applied to a product row)

`∀` binder, zero `sorry`, named property, rustc fn, not wrap-factory.
`rfl` on concrete inputs (`wal_commit_plan true false`,
`leftover_txn_is_aborted = ok true`) does **not** move a row off `model`.
Layer only goes `model → atom → close`. Corollary `unfold`s the production
fn, not only the modelo.

## Not this

Trampoline `if` while `leftover_next` names P0.* or P1.1–P1.4.
`media_durable` / `forall_schedules` / `lock_interleavings` → true.
Dump `db.rs`. Mint cartoon. Skip Montanha / L28.
