[workspace]

[package]
name = "pedra-aeneas-pin-kernel"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "Standalone crate whose [lib] path is production pin_kernel.rs"

[lib]
path = "../../../crates/pedradb-journal/src/pin_kernel.rs"
