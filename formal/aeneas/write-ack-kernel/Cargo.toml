[workspace]

[package]
name = "pedra-aeneas-write-ack-kernel"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "Shim exposing production write_ack_kernel.rs (+ d1_modelo + env_crash + wal_state) to Charon"

[lib]
path = "src/lib.rs"
