[workspace]

[package]
name = "pedra-aeneas-fail-closed-kernel"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "Standalone crate whose [lib] path is production http fail_closed.rs"

[lib]
path = "../../../crates/pedradb-http/src/fail_closed_kernel.rs"
