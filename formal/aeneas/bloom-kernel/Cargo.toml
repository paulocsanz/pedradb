[workspace]

[package]
name = "pedra-aeneas-bloom-kernel"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "Standalone crate whose [lib] path is production bloom.rs"

[lib]
path = "../../../crates/pedradb-core/src/bloom.rs"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }
