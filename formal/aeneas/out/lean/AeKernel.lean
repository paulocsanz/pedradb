-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [pedra_aeneas_ae_kernel]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

namespace pedra_aeneas_ae_kernel

/-- [pedra_aeneas_ae_kernel::ae_prev_log_ok]:
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 28:0-41:1
    Visibility: public -/
def ae_prev_log_ok
  (prev_log_index : Std.U64) (prev_log_term : Std.U64)
  (last_log_index : Std.U64) (log_term_at_prev : Std.U64) :
  Result Bool
  := do
  if prev_log_index = 0#u64
  then ok true
  else
    if last_log_index < prev_log_index
    then ok false
    else ok (log_term_at_prev = prev_log_term)

/-- [pedra_aeneas_ae_kernel::AeEntryAction]
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 45:0-54:1
    Visibility: public -/
@[discriminant isize]
inductive AeEntryAction where
| Keep : AeEntryAction
| Append : AeEntryAction
| TruncateAndInstall : AeEntryAction
| Refuse : AeEntryAction

/-- [pedra_aeneas_ae_kernel::{impl core::fmt::Debug for pedra_aeneas_ae_kernel::AeEntryAction}::fmt]:
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 44:9-44:14
    Visibility: public -/
def AeEntryAction.Insts.CoreFmtDebug.fmt
  (self : AeEntryAction) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  match self with
  | AeEntryAction.Keep => core.fmt.Formatter.write_str f (toStr "Keep")
  | AeEntryAction.Append => core.fmt.Formatter.write_str f (toStr "Append")
  | AeEntryAction.TruncateAndInstall =>
    core.fmt.Formatter.write_str f (toStr "TruncateAndInstall")
  | AeEntryAction.Refuse => core.fmt.Formatter.write_str f (toStr "Refuse")

/-- Trait implementation: [pedra_aeneas_ae_kernel::{impl core::fmt::Debug for pedra_aeneas_ae_kernel::AeEntryAction}]
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 44:9-44:14 -/
@[reducible]
def AeEntryAction.Insts.CoreFmtDebug : core.fmt.Debug AeEntryAction := {
  fmt := AeEntryAction.Insts.CoreFmtDebug.fmt
}

/-- [pedra_aeneas_ae_kernel::{impl core::clone::Clone for pedra_aeneas_ae_kernel::AeEntryAction}::clone]:
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 44:16-44:21
    Visibility: public -/
def AeEntryAction.Insts.CoreCloneClone.clone
  (self : AeEntryAction) : Result AeEntryAction := do
  ok self

/-- Trait implementation: [pedra_aeneas_ae_kernel::{impl core::clone::Clone for pedra_aeneas_ae_kernel::AeEntryAction}]
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 44:16-44:21 -/
@[reducible]
def AeEntryAction.Insts.CoreCloneClone : core.clone.Clone AeEntryAction := {
  clone := AeEntryAction.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [pedra_aeneas_ae_kernel::{impl core::marker::Copy for pedra_aeneas_ae_kernel::AeEntryAction}]
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 44:23-44:27 -/
@[reducible]
def AeEntryAction.Insts.CoreMarkerCopy : core.marker.Copy AeEntryAction := {
  cloneInst := AeEntryAction.Insts.CoreCloneClone
}

/-- Trait implementation: [pedra_aeneas_ae_kernel::{impl core::marker::StructuralPartialEq for pedra_aeneas_ae_kernel::AeEntryAction}]
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 44:29-44:38 -/
@[reducible]
def AeEntryAction.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq AeEntryAction := {
}

/-- [pedra_aeneas_ae_kernel::{impl core::cmp::PartialEq<pedra_aeneas_ae_kernel::AeEntryAction> for pedra_aeneas_ae_kernel::AeEntryAction}::eq]:
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 44:29-44:38
    Visibility: public -/
def AeEntryAction.Insts.CoreCmpPartialEqAeEntryAction.eq
  (self : AeEntryAction) (other : AeEntryAction) : Result Bool := do
  let self1 := read_discriminant self
  let other1 := read_discriminant other
  ok (self1 = other1)

/-- Trait implementation: [pedra_aeneas_ae_kernel::{impl core::cmp::PartialEq<pedra_aeneas_ae_kernel::AeEntryAction> for pedra_aeneas_ae_kernel::AeEntryAction}]
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 44:29-44:38 -/
@[reducible]
def AeEntryAction.Insts.CoreCmpPartialEqAeEntryAction : core.cmp.PartialEq
  AeEntryAction AeEntryAction := {
  eq := AeEntryAction.Insts.CoreCmpPartialEqAeEntryAction.eq
}

/-- [pedra_aeneas_ae_kernel::{impl core::cmp::Eq for pedra_aeneas_ae_kernel::AeEntryAction}::assert_fields_are_eq]:
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 44:40-44:42
    Visibility: public -/
def AeEntryAction.Insts.CoreCmpEq.assert_fields_are_eq
  (self : AeEntryAction) : Result Unit := do
  ok ()

/-- Trait implementation: [pedra_aeneas_ae_kernel::{impl core::cmp::Eq for pedra_aeneas_ae_kernel::AeEntryAction}]
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 44:40-44:42 -/
@[reducible]
def AeEntryAction.Insts.CoreCmpEq : core.cmp.Eq AeEntryAction := {
  partialEqInst := AeEntryAction.Insts.CoreCmpPartialEqAeEntryAction
  assert_fields_are_eq := AeEntryAction.Insts.CoreCmpEq.assert_fields_are_eq
}

/-- [pedra_aeneas_ae_kernel::ae_entry_action]:
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 71:0-98:1
    Visibility: public -/
def ae_entry_action
  (entry_index : Std.U64) (entry_term : Std.U64)
  (existing_term : Option Std.U64) (commit_index : Std.U64)
  (last_log_index : Std.U64) :
  Result AeEntryAction
  := do
  match existing_term with
  | none =>
    let expect ← lift (core.num.U64.saturating_add last_log_index 1#u64)
    if entry_index != expect
    then ok AeEntryAction.Refuse
    else ok AeEntryAction.Append
  | some t =>
    if t = entry_term
    then ok AeEntryAction.Keep
    else
      if entry_index <= commit_index
      then ok AeEntryAction.Refuse
      else ok AeEntryAction.TruncateAndInstall

/-- [pedra_aeneas_ae_kernel::ae_f16_safe]:
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 102:0-131:1
    Visibility: public -/
def ae_f16_safe
  (entry_index : Std.U64) (entry_term : Std.U64)
  (existing_term : Option Std.U64) (commit_index : Std.U64)
  (last_log_index : Std.U64) (action : AeEntryAction) :
  Result Bool
  := do
  let b ←
    match action with
    | AeEntryAction.Keep => ok false
    | AeEntryAction.Append => ok false
    | AeEntryAction.TruncateAndInstall => ok true
    | AeEntryAction.Refuse => ok false
  if b
  then
    if entry_index <= commit_index
    then ok false
    else
      let b1 ←
        match action with
        | AeEntryAction.Keep => ok false
        | AeEntryAction.Append => ok true
        | AeEntryAction.TruncateAndInstall => ok false
        | AeEntryAction.Refuse => ok false
      if b1
      then
        let b2 := core.option.Option.is_some existing_term
        if b2
        then ok false
        else
          let i ← lift (core.num.U64.saturating_add last_log_index 1#u64)
          if entry_index != i
          then ok false
          else
            match existing_term with
            | none => ok true
            | some t =>
              if t != entry_term
              then
                if entry_index <= commit_index
                then
                  match action with
                  | AeEntryAction.Keep => ok false
                  | AeEntryAction.Append => ok false
                  | AeEntryAction.TruncateAndInstall => ok false
                  | AeEntryAction.Refuse => ok true
                else ok true
              else ok true
      else
        match existing_term with
        | none => ok true
        | some t =>
          if t != entry_term
          then
            if entry_index <= commit_index
            then
              match action with
              | AeEntryAction.Keep => ok false
              | AeEntryAction.Append => ok false
              | AeEntryAction.TruncateAndInstall => ok false
              | AeEntryAction.Refuse => ok true
            else ok true
          else ok true
  else
    let b1 ←
      match action with
      | AeEntryAction.Keep => ok false
      | AeEntryAction.Append => ok true
      | AeEntryAction.TruncateAndInstall => ok false
      | AeEntryAction.Refuse => ok false
    if b1
    then
      let b2 := core.option.Option.is_some existing_term
      if b2
      then ok false
      else
        let i ← lift (core.num.U64.saturating_add last_log_index 1#u64)
        if entry_index != i
        then ok false
        else
          match existing_term with
          | none => ok true
          | some t =>
            if t != entry_term
            then
              if entry_index <= commit_index
              then
                match action with
                | AeEntryAction.Keep => ok false
                | AeEntryAction.Append => ok false
                | AeEntryAction.TruncateAndInstall => ok false
                | AeEntryAction.Refuse => ok true
              else ok true
            else ok true
    else
      match existing_term with
      | none => ok true
      | some t =>
        if t != entry_term
        then
          if entry_index <= commit_index
          then
            match action with
            | AeEntryAction.Keep => ok false
            | AeEntryAction.Append => ok false
            | AeEntryAction.TruncateAndInstall => ok false
            | AeEntryAction.Refuse => ok true
          else ok true
        else ok true

/-- [pedra_aeneas_ae_kernel::ae_ack_success]:
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 137:0-139:1
    Visibility: public -/
def ae_ack_success (log_dirty : Bool) (persist_ok : Bool) : Result Bool := do
  if log_dirty
  then ok persist_ok
  else ok true

/-- [pedra_aeneas_ae_kernel::ae_ack_success_as_is]:
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 143:0-145:1
    Visibility: public -/
def ae_ack_success_as_is
  (_log_dirty : Bool) (_persist_ok : Bool) : Result Bool := do
  ok true

/-- [pedra_aeneas_ae_kernel::ae_entry_action_as_is_rewrite_committed]:
    Source: '../../../crates/pedradb-raft/src/ae_kernel.rs', lines 150:0-169:1
    Visibility: public -/
def ae_entry_action_as_is_rewrite_committed
  (entry_index : Std.U64) (entry_term : Std.U64)
  (existing_term : Option Std.U64) (_commit_index : Std.U64)
  (last_log_index : Std.U64) :
  Result AeEntryAction
  := do
  match existing_term with
  | none =>
    let expect ← lift (core.num.U64.saturating_add last_log_index 1#u64)
    if entry_index != expect
    then ok AeEntryAction.Refuse
    else ok AeEntryAction.Append
  | some t =>
    if t = entry_term
    then ok AeEntryAction.Keep
    else ok AeEntryAction.TruncateAndInstall

end pedra_aeneas_ae_kernel
