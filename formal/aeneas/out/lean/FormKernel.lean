-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [pedra_aeneas_form_kernel]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

/- You can remove the following line by using the CLI option `-all-computable`: -/
noncomputable section

namespace pedra_aeneas_form_kernel

/-- [core::slice::iter::{impl core::iter::traits::iterator::Iterator<&'a T> for core::slice::iter::Iter<'a, T>}::any]:
    Source: '/rustc/library/core/src/slice/iter/macros.rs', lines 326:12-329:45
    Name pattern: [core::slice::iter::{core::iter::traits::iterator::Iterator<core::slice::iter::Iter<'a, @T>, &'a @T>}::any]
    Visibility: public -/
@[rust_fun
  "core::slice::iter::{core::iter::traits::iterator::Iterator<core::slice::iter::Iter<'a, @T>, &'a @T>}::any"]
axiom core.slice.iter.Iter.Insts.CoreIterTraitsIteratorIteratorSharedAT.any
  {T : Type} {F : Type} (opsfunctionFnMutFTupleSharedATBoolInst :
  core.ops.function.FnMut F T Bool) :
  core.slice.iter.Iter T → F → Result (Bool × (core.slice.iter.Iter T))

/-- [core::str::{str}::is_empty]:
    Source: '/rustc/library/core/src/str/mod.rs', lines 173:4-173:40
    Name pattern: [core::str::{str}::is_empty]
    Visibility: public -/
@[rust_fun "core::str::{str}::is_empty"]
axiom core.str.Str.is_empty : Str → Result Bool

/-- [core::str::{str}::as_bytes]:
    Source: '/rustc/library/core/src/str/mod.rs', lines 498:4-498:41
    Name pattern: [core::str::{str}::as_bytes]
    Visibility: public -/
@[rust_fun "core::str::{str}::as_bytes"]
axiom core.str.Str.as_bytes : Str → Result (Slice Std.U8)

/-- [core::str::{str}::contains]:
    Source: '/rustc/library/core/src/str/mod.rs', lines 1367:4-1367:54
    Name pattern: [core::str::{str}::contains]
    Visibility: public -/
@[rust_fun "core::str::{str}::contains"]
axiom core.str.Str.contains : Str → Char → Result Bool

/-- [core::str::traits::{impl core::cmp::PartialEq<str> for str}::eq]:
    Source: '/rustc/library/core/src/str/traits.rs', lines 29:4-29:37
    Name pattern: [core::str::traits::{core::cmp::PartialEq<str, str>}::eq]
    Visibility: public -/
@[rust_fun "core::str::traits::{core::cmp::PartialEq<str, str>}::eq"]
axiom Str.Insts.CoreCmpPartialEqStr.eq : Str → Str → Result Bool

/-- Trait implementation: [core::str::traits::{impl core::cmp::PartialEq<str> for str}]
    Source: '/rustc/library/core/src/str/traits.rs', lines 27:0-27:28
    Name pattern: [core::cmp::PartialEq<str, str>] -/
@[reducible, rust_trait_impl "core::cmp::PartialEq<str, str>"]
impl_def Str.Insts.CoreCmpPartialEqStr : core.cmp.PartialEq Str Str := {
  eq := Str.Insts.CoreCmpPartialEqStr.eq
  ne := core.cmp.PartialEq.ne.trait_default Str.Insts.CoreCmpPartialEqStr
}

/-- [alloc::vec::partial_eq::{impl core::cmp::PartialEq<&'_0 [U]> for alloc::vec::Vec<T>}::eq]:
    Source: '/rustc/library/alloc/src/vec/partial_eq.rs', lines 15:12-15:46
    Name pattern: [alloc::vec::partial_eq::{core::cmp::PartialEq<alloc::vec::Vec<@T>, &'0 [@U]>}::eq]
    Visibility: public -/
@[rust_fun
  "alloc::vec::partial_eq::{core::cmp::PartialEq<alloc::vec::Vec<@T>, &'0 [@U]>}::eq"]
axiom alloc.vec.Vec.Insts.CoreCmpPartialEqShared0Slice.eq
  {T : Type} {U : Type} (A : Type) (corecmpPartialEqInst : core.cmp.PartialEq T
  U) :
  alloc.vec.Vec T → Slice U → Result Bool

/-- [pedra_aeneas_form_kernel::form_plus_byte]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 54:0-56:1
    Visibility: public -/
def form_plus_byte (b : Std.U8) : Result Std.U8 := do
  if b = 43#u8
  then ok 32#u8
  else ok b

/-- [pedra_aeneas_form_kernel::form_plus_byte_as_is]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 61:0-63:1
    Visibility: public -/
def form_plus_byte_as_is (b : Std.U8) : Result Std.U8 := do
  ok b

/-- [pedra_aeneas_form_kernel::plus_before_percent]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 68:0-70:1
    Visibility: public -/
def plus_before_percent : Result Bool := do
  ok true

/-- [pedra_aeneas_form_kernel::from_hex]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 75:0-82:1
    Visibility: public -/
def from_hex (c : Std.U8) : Result (Option Std.U8) := do
  if 48#u8 <= c
  then
    if c <= 57#u8
    then let i ← c - 48#u8
         ok (some i)
    else
      if 97#u8 <= c
      then
        if c <= 102#u8
        then let i ← c - 97#u8
             let i1 ← i + 10#u8
             ok (some i1)
        else
          if 65#u8 <= c
          then
            if c <= 70#u8
            then let i ← c - 65#u8
                 let i1 ← i + 10#u8
                 ok (some i1)
            else ok none
          else ok none
      else
        if 65#u8 <= c
        then
          if c <= 70#u8
          then let i ← c - 65#u8
               let i1 ← i + 10#u8
               ok (some i1)
          else ok none
        else ok none
  else
    if 97#u8 <= c
    then
      if c <= 102#u8
      then let i ← c - 97#u8
           let i1 ← i + 10#u8
           ok (some i1)
      else
        if 65#u8 <= c
        then
          if c <= 70#u8
          then let i ← c - 65#u8
               let i1 ← i + 10#u8
               ok (some i1)
          else ok none
        else ok none
    else
      if 65#u8 <= c
      then
        if c <= 70#u8
        then let i ← c - 65#u8
             let i1 ← i + 10#u8
             ok (some i1)
        else ok none
      else ok none

/-- [pedra_aeneas_form_kernel::form_decode]: loop body 0:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 1:0-106:5
    Visibility: public -/
@[rust_loop_body]
def form_decode_loop.body
  (b : Slice Std.U8) (out : alloc.vec.Vec Std.U8) (i : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec Std.U8) × Std.Usize) (alloc.vec.Vec
    Std.U8))
  := do
  let i1 := Slice.len b
  if i < i1
  then
    let b1 ← plus_before_percent
    if b1
    then
      let i2 ← Slice.index_usize b i
      if i2 = 43#u8
      then
        let i3 ← form_plus_byte i2
        let out1 ← alloc.vec.Vec.push out i3
        let i4 ← i + 1#usize
        ok (cont (out1, i4))
      else
        if i2 = 37#u8
        then
          let i3 ← i + 2#usize
          let i4 := Slice.len b
          if i3 < i4
          then
            let i5 ← i + 1#usize
            let i6 ← Slice.index_usize b i5
            let o ← from_hex i6
            let i7 ← Slice.index_usize b i3
            let o1 ← from_hex i7
            match o with
            | none =>
              let out1 ← alloc.vec.Vec.push out i2
              ok (cont (out1, i5))
            | some h =>
              match o1 with
              | none =>
                let out1 ← alloc.vec.Vec.push out i2
                ok (cont (out1, i5))
              | some l =>
                let i8 ← h <<< 4#i32
                let i9 ← lift (i8 ||| l)
                let out1 ← alloc.vec.Vec.push out i9
                let i10 ← i + 3#usize
                ok (cont (out1, i10))
          else
            let out1 ← alloc.vec.Vec.push out i2
            let i5 ← i + 1#usize
            ok (cont (out1, i5))
        else
          let out1 ← alloc.vec.Vec.push out i2
          let i3 ← i + 1#usize
          ok (cont (out1, i3))
    else
      let i2 ← Slice.index_usize b i
      if i2 = 37#u8
      then
        let i3 ← i + 2#usize
        let i4 := Slice.len b
        if i3 < i4
        then
          let i5 ← i + 1#usize
          let i6 ← Slice.index_usize b i5
          let o ← from_hex i6
          let i7 ← Slice.index_usize b i3
          let o1 ← from_hex i7
          match o with
          | none => let out1 ← alloc.vec.Vec.push out i2
                    ok (cont (out1, i5))
          | some h =>
            match o1 with
            | none =>
              let out1 ← alloc.vec.Vec.push out i2
              ok (cont (out1, i5))
            | some l =>
              let i8 ← h <<< 4#i32
              let i9 ← lift (i8 ||| l)
              let out1 ← alloc.vec.Vec.push out i9
              let i10 ← i + 3#usize
              ok (cont (out1, i10))
        else
          let out1 ← alloc.vec.Vec.push out i2
          let i5 ← i + 1#usize
          ok (cont (out1, i5))
      else
        let out1 ← alloc.vec.Vec.push out i2
        let i3 ← i + 1#usize
        ok (cont (out1, i3))
  else ok (done out)

/-- [pedra_aeneas_form_kernel::form_decode]: loop 0:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 1:0-106:5
    Visibility: public -/
@[rust_loop]
def form_decode_loop
  (b : Slice Std.U8) (out : alloc.vec.Vec Std.U8) (i : Std.Usize) :
  Result (alloc.vec.Vec Std.U8)
  := do
  loop
    (fun (out1, i1) => form_decode_loop.body b out1 i1)
    (out, i)

/-- [pedra_aeneas_form_kernel::form_decode]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 87:0-108:1
    Visibility: public -/
def form_decode (s : Str) : Result (alloc.vec.Vec Std.U8) := do
  let b ← core.str.Str.as_bytes s
  let i := Slice.len b
  let out := alloc.vec.Vec.with_capacity Std.U8 i
  form_decode_loop b out 0#usize

/-- [pedra_aeneas_form_kernel::query_values_conflict::closure]
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 116:46-116:62 -/
@[reducible]
def query_values_conflict.closure := Str

/-- [pedra_aeneas_form_kernel::query_values_conflict::{impl core::ops::function::FnMut<(&'_ &'_ str,), bool> for pedra_aeneas_form_kernel::query_values_conflict::closure<'_0, '_1>}::call_mut]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 116:46-116:62 -/
def
  query_values_conflict.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedStrBool.call_mut
  (c : query_values_conflict.closure) (tupled_args : Str) :
  Result (Bool × query_values_conflict.closure)
  := do
  let b ←
    core.cmp.impls.PartialEqShared.ne Str.Insts.CoreCmpPartialEqStr tupled_args
      c
  ok (b, c)

/-- [pedra_aeneas_form_kernel::query_values_conflict::{impl core::ops::function::FnOnce<(&'_ &'_ str,), bool> for pedra_aeneas_form_kernel::query_values_conflict::closure<'_0, '_1>}::call_once]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 116:46-116:62 -/
def
  query_values_conflict.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedStrBool.call_once
  (c : query_values_conflict.closure) (s : Str) : Result Bool := do
  let (b, _) ←
    query_values_conflict.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedStrBool.call_mut
      c s
  ok b

/-- Trait implementation: [pedra_aeneas_form_kernel::query_values_conflict::{impl core::ops::function::FnOnce<(&'_ &'_ str,), bool> for pedra_aeneas_form_kernel::query_values_conflict::closure<'_0, '_1>}]
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 116:46-116:62 -/
@[reducible]
def
  query_values_conflict.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedStrBool
  : core.ops.function.FnOnce query_values_conflict.closure Str Bool := {
  call_once :=
    query_values_conflict.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedStrBool.call_once
}

/-- Trait implementation: [pedra_aeneas_form_kernel::query_values_conflict::{impl core::ops::function::FnMut<(&'_ &'_ str,), bool> for pedra_aeneas_form_kernel::query_values_conflict::closure<'_0, '_1>}]
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 116:46-116:62 -/
@[reducible]
def
  query_values_conflict.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedStrBool
  : core.ops.function.FnMut query_values_conflict.closure Str Bool := {
  FnOnceInst :=
    query_values_conflict.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedStrBool
  call_mut :=
    query_values_conflict.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedStrBool.call_mut
}

/-- [pedra_aeneas_form_kernel::query_values_conflict]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 113:0-118:1
    Visibility: public -/
@[rust_loop_body]
def query_values_conflict_loop.body
  (values : Slice Str) (first : Str) (i : Std.Usize) :
  Result (ControlFlow Std.Usize Bool)
  := do
  let n := Slice.len values
  if i < n
  then
    let v ← Slice.index_usize values i
    let eq ← Str.Insts.CoreCmpPartialEqStr.eq first v
    match eq with
    | true =>
      let i1 ← i + 1#usize
      ok (cont i1)
    | false => ok (done true)
  else ok (done false)

@[rust_loop]
def query_values_conflict_loop
  (values : Slice Str) (first : Str) (i : Std.Usize) : Result Bool := do
  loop (fun i1 => query_values_conflict_loop.body values first i1) i

def query_values_conflict (values : Slice Str) : Result Bool := do
  let n := Slice.len values
  if n < 2#usize
  then ok false
  else
    let first ← Slice.index_usize values 0#usize
    query_values_conflict_loop values first 1#usize

/-- [pedra_aeneas_form_kernel::query_values_conflict_as_is]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 123:0-125:1
    Visibility: public -/
def query_values_conflict_as_is (_values : Slice Str) : Result Bool := do
  ok false

/-- [pedra_aeneas_form_kernel::query_u64_conflict]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 130:0-132:1
    Visibility: public -/
def query_u64_conflict (a : Std.U64) (b : Std.U64) : Result Bool := do
  ok (a != b)

/-- [pedra_aeneas_form_kernel::query_u64_conflict_as_is]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 137:0-139:1
    Visibility: public -/
def query_u64_conflict_as_is (a : Std.U64) (b : Std.U64) : Result Bool := do
  ok false

/-- [pedra_aeneas_form_kernel::query_part_is_bare_name]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 144:0-146:1
    Visibility: public -/
def query_part_is_bare_name (part : Str) (key : Str) : Result Bool := do
  let b ← core.str.Str.is_empty part
  if b
  then ok false
  else
    let b1 ←
      core.str.Str.contains part '='
    if b1
    then ok false
    else
      let v ← form_decode part
      let s ← core.str.Str.as_bytes key
      alloc.vec.Vec.Insts.CoreCmpPartialEqShared0Slice.eq Global
        core.cmp.PartialEqU8 v s

/-- [pedra_aeneas_form_kernel::query_part_is_bare_name_as_is]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 151:0-153:1
    Visibility: public -/
def query_part_is_bare_name_as_is
  (_part : Str) (_key : Str) : Result Bool := do
  ok false

/-- [pedra_aeneas_form_kernel::form_decode_as_is]: loop body 0:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 1:0-172:5
    Visibility: public -/
@[rust_loop_body]
def form_decode_as_is_loop.body
  (b : Slice Std.U8) (out : alloc.vec.Vec Std.U8) (i : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec Std.U8) × Std.Usize) (alloc.vec.Vec
    Std.U8))
  := do
  let i1 := Slice.len b
  if i < i1
  then
    let i2 ← Slice.index_usize b i
    if i2 = 37#u8
    then
      let i3 ← i + 2#usize
      let i4 := Slice.len b
      if i3 < i4
      then
        let i5 ← i + 1#usize
        let i6 ← Slice.index_usize b i5
        let o ← from_hex i6
        let i7 ← Slice.index_usize b i3
        let o1 ← from_hex i7
        match o with
        | none => let out1 ← alloc.vec.Vec.push out i2
                  ok (cont (out1, i5))
        | some h =>
          match o1 with
          | none => let out1 ← alloc.vec.Vec.push out i2
                    ok (cont (out1, i5))
          | some l =>
            let i8 ← h <<< 4#i32
            let i9 ← lift (i8 ||| l)
            let out1 ← alloc.vec.Vec.push out i9
            let i10 ← i + 3#usize
            ok (cont (out1, i10))
      else
        let out1 ← alloc.vec.Vec.push out i2
        let i5 ← i + 1#usize
        ok (cont (out1, i5))
    else
      let out1 ← alloc.vec.Vec.push out i2
      let i3 ← i + 1#usize
      ok (cont (out1, i3))
  else ok (done out)

/-- [pedra_aeneas_form_kernel::form_decode_as_is]: loop 0:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 1:0-172:5
    Visibility: public -/
@[rust_loop]
def form_decode_as_is_loop
  (b : Slice Std.U8) (out : alloc.vec.Vec Std.U8) (i : Std.Usize) :
  Result (alloc.vec.Vec Std.U8)
  := do
  loop
    (fun (out1, i1) => form_decode_as_is_loop.body b out1 i1)
    (out, i)

/-- [pedra_aeneas_form_kernel::form_decode_as_is]:
    Source: '../../../crates/pedradb-http/src/form_kernel.rs', lines 158:0-174:1
    Visibility: public -/
def form_decode_as_is (s : Str) : Result (alloc.vec.Vec Std.U8) := do
  let b ← core.str.Str.as_bytes s
  let i := Slice.len b
  let out := alloc.vec.Vec.with_capacity Std.U8 i
  form_decode_as_is_loop b out 0#usize

end pedra_aeneas_form_kernel
