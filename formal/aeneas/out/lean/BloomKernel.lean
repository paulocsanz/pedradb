-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [pedra_aeneas_bloom_kernel]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

/- You can remove the following line by using the CLI option `-all-computable`: -/
noncomputable section

namespace pedra_aeneas_bloom_kernel

/-- Trait implementation: [core::fmt::num::imp::{impl core::fmt::Display for usize}]
    Source: '/rustc/library/core/src/fmt/num.rs', lines 134:8-134:39
    Name pattern: [core::fmt::Display<usize>] -/
@[reducible, rust_trait_impl "core::fmt::Display<usize>"]
def Usize.Insts.CoreFmtDisplay : core.fmt.Display Std.Usize := {
  fmt := core.fmt.num.imp.DisplayUsize.fmt
}

/-- Trait implementation: [core::fmt::num::imp::{impl core::fmt::Display for u32}]
    Source: '/rustc/library/core/src/fmt/num.rs', lines 134:8-134:39
    Name pattern: [core::fmt::Display<u32>] -/
@[reducible, rust_trait_impl "core::fmt::Display<u32>"]
def U32.Insts.CoreFmtDisplay : core.fmt.Display Std.U32 := {
  fmt := core.fmt.num.imp.DisplayU32.fmt
}

/-- [core::hint::must_use]:
    Source: '/rustc/library/core/src/hint.rs', lines 613:0-613:39
    Name pattern: [core::hint::must_use]
    Visibility: public -/
@[rust_fun "core::hint::must_use"]
axiom core.hint.must_use {T : Type} : T → Result T

/-- [core::num::{usize}::saturating_mul]:
    Source: '/rustc/library/core/src/num/uint_macros.rs', lines 2516:8-2516:60
    Name pattern: [core::num::{usize}::saturating_mul]
    Visibility: public -/
@[rust_fun "core::num::{usize}::saturating_mul"]
axiom core.num.Usize.saturating_mul
  : Std.Usize → Std.Usize → Result Std.Usize

/-- [core::num::{u64}::div_ceil]:
    Source: '/rustc/library/core/src/num/uint_macros.rs', lines 3755:8-3755:54
    Name pattern: [core::num::{u64}::div_ceil]
    Visibility: public -/
@[rust_fun "core::num::{u64}::div_ceil"]
axiom core.num.U64.div_ceil : Std.U64 → Std.U64 → Result Std.U64

/-- [core::num::{usize}::div_ceil]:
    Source: '/rustc/library/core/src/num/uint_macros.rs', lines 3755:8-3755:54
    Name pattern: [core::num::{usize}::div_ceil]
    Visibility: public -/
@[rust_fun "core::num::{usize}::div_ceil"]
axiom core.num.Usize.div_ceil : Std.Usize → Std.Usize → Result Std.Usize

/-- [core::ops::range::{core::ops::range::RangeInclusive<Idx>}::contains]:
    Source: '/rustc/library/core/src/ops/range.rs', lines 522:4-525:44
    Name pattern: [core::ops::range::{core::ops::range::RangeInclusive<@Idx>}::contains]
    Visibility: public -/
@[rust_fun
  "core::ops::range::{core::ops::range::RangeInclusive<@Idx>}::contains"]
axiom core.ops.range.RangeInclusive.contains
  {Idx : Type} {U : Type} (cmpPartialOrdInst : core.cmp.PartialOrd Idx Idx)
  (cmpPartialOrdInst1 : core.cmp.PartialOrd Idx U) (cmpPartialOrdInst2 :
  core.cmp.PartialOrd U Idx) :
  core.ops.range.RangeInclusive Idx → U → Result Bool

/-- [core::result::{core::result::Result<T, E>}::unwrap_or]:
    Source: '/rustc/library/core/src/result.rs', lines 1590:4-1593:28
    Name pattern: [core::result::{core::result::Result<@T, @E>}::unwrap_or]
    Visibility: public -/
@[rust_fun "core::result::{core::result::Result<@T, @E>}::unwrap_or"]
axiom core.result.Result.unwrap_or
  {T : Type} {E : Type} : core.result.Result T E → T → Result T

/-- [alloc::fmt::format]:
    Source: '/rustc/library/alloc/src/fmt.rs', lines 649:0-649:52
    Name pattern: [alloc::fmt::format]
    Visibility: public -/
@[rust_fun "alloc::fmt::format"]
axiom alloc.fmt.format : core.fmt.Arguments → Result String

/-- [alloc::string::{impl core::convert::From<&'_0 str> for alloc::string::String}::from]:
    Source: '/rustc/library/alloc/src/string.rs', lines 3126:4-3126:30
    Name pattern: [alloc::string::{core::convert::From<alloc::string::String, &'0 str>}::from]
    Visibility: public -/
@[rust_fun
  "alloc::string::{core::convert::From<alloc::string::String, &'0 str>}::from"]
axiom alloc.string.String.Insts.CoreConvertFromShared0Str.from
  : Str → Result String

/-- Trait implementation: [alloc::string::{impl core::convert::From<&'_0 str> for alloc::string::String}]
    Source: '/rustc/library/alloc/src/string.rs', lines 3121:0-3121:26
    Name pattern: [core::convert::From<alloc::string::String, &'0 str>] -/
@[reducible, rust_trait_impl
  "core::convert::From<alloc::string::String, &'0 str>"]
def alloc.string.String.Insts.CoreConvertFromShared0Str : core.convert.From
  String Str := {
  «from» := alloc.string.String.Insts.CoreConvertFromShared0Str.from
}

/-- [alloc::vec::{alloc::vec::Vec<T>}::is_empty]:
    Source: '/rustc/library/alloc/src/vec/mod.rs', lines 3085:4-3085:40
    Name pattern: [alloc::vec::{alloc::vec::Vec<@T>}::is_empty]
    Visibility: public -/
@[rust_fun "alloc::vec::{alloc::vec::Vec<@T>}::is_empty"]
axiom alloc.vec.Vec.is_empty
  {T : Type} (A : Type) : alloc.vec.Vec T → Result Bool

/-- [pedra_aeneas_bloom_kernel::DEFAULT_BITS_PER_KEY]
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 7:0-7:43
    Visibility: public -/
@[global_simps, irreducible] def DEFAULT_BITS_PER_KEY : Std.Usize := 10#usize

/-- [pedra_aeneas_bloom_kernel::MAX_K]
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 13:0-13:26
    Visibility: public -/
@[global_simps, irreducible] def MAX_K : Std.U32 := 30#u32

/-- [pedra_aeneas_bloom_kernel::bloom_header_ok]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 21:0-25:1
    Visibility: public -/
def bloom_header_ok
  (nbits : Std.U32) (k : Std.U32) (nbytes : Std.U32) (residual : Std.U64) :
  Result Bool
  := do
  let ri ← core.ops.range.RangeInclusive.new 1#u32 MAX_K
  let b ←
    core.ops.range.RangeInclusive.contains core.cmp.PartialOrdU32
      core.cmp.PartialOrdU32 core.cmp.PartialOrdU32 ri k
  if b
  then
    let i ← lift (core.convert.num.FromU64U32.from nbytes)
    let i1 ← lift (core.convert.num.FromU64U32.from nbits)
    let i2 ← core.num.U64.div_ceil i1 8#u64
    if i >= i2
    then
      let i3 ← lift (core.convert.num.FromU64U32.from nbytes)
      ok (i3 <= residual)
    else ok false
  else ok false

/-- [pedra_aeneas_bloom_kernel::bloom_header_ok_as_is]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 29:0-31:1
    Visibility: public -/
def bloom_header_ok_as_is
  (nbits : Std.U32) (_k : Std.U32) (nbytes : Std.U32) (residual : Std.U64) :
  Result Bool
  := do
  let i ← lift (core.convert.num.FromU64U32.from nbytes)
  let i1 ← lift (core.convert.num.FromU64U32.from nbits)
  let i2 ← core.num.U64.div_ceil i1 8#u64
  if i >= i2
  then
    let i3 ← lift (core.convert.num.FromU64U32.from nbytes)
    ok (i3 <= residual)
  else ok false

/-- [pedra_aeneas_bloom_kernel::BloomFilter]
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 35:0-41:1
    Visibility: public -/
structure BloomFilter where
  bits : alloc.vec.Vec Std.U8
  nbits : Std.U32
  k : Std.U32

/-- [pedra_aeneas_bloom_kernel::{impl core::fmt::Debug for pedra_aeneas_bloom_kernel::BloomFilter}::fmt]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 34:9-34:14
    Visibility: public -/
def BloomFilter.Insts.CoreFmtDebug.fmt
  (self : BloomFilter) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ (core.fmt.DebugVec core.fmt.DebugU8) self.bits
  let dyn1 := Dyn.mk _ core.fmt.DebugU32 self.nbits
  let dyn2 := Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) self.k
  core.fmt.Formatter.debug_struct_field3_finish f (toStr "BloomFilter") (toStr
    "bits") dyn (toStr "nbits") dyn1 (toStr "k") dyn2

/-- Trait implementation: [pedra_aeneas_bloom_kernel::{impl core::fmt::Debug for pedra_aeneas_bloom_kernel::BloomFilter}]
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 34:9-34:14 -/
@[reducible]
def BloomFilter.Insts.CoreFmtDebug : core.fmt.Debug BloomFilter := {
  fmt := BloomFilter.Insts.CoreFmtDebug.fmt
}

/-- [pedra_aeneas_bloom_kernel::{impl core::clone::Clone for pedra_aeneas_bloom_kernel::BloomFilter}::clone]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 34:16-34:21
    Visibility: public -/
def BloomFilter.Insts.CoreCloneClone.clone
  (self : BloomFilter) : Result BloomFilter := do
  let v ← alloc.vec.CloneVec.clone core.clone.CloneU8 self.bits
  let i ← lift (core.clone.impls.CloneU32.clone self.nbits)
  let i1 ← lift (core.clone.impls.CloneU32.clone self.k)
  ok { bits := v, nbits := i, k := i1 }

/-- Trait implementation: [pedra_aeneas_bloom_kernel::{impl core::clone::Clone for pedra_aeneas_bloom_kernel::BloomFilter}]
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 34:16-34:21 -/
@[reducible]
def BloomFilter.Insts.CoreCloneClone : core.clone.Clone BloomFilter := {
  clone := BloomFilter.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [pedra_aeneas_bloom_kernel::{impl core::marker::StructuralPartialEq for pedra_aeneas_bloom_kernel::BloomFilter}]
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 34:23-34:32 -/
@[reducible]
def BloomFilter.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq BloomFilter := {
}

/-- [pedra_aeneas_bloom_kernel::{impl core::cmp::PartialEq<pedra_aeneas_bloom_kernel::BloomFilter> for pedra_aeneas_bloom_kernel::BloomFilter}::eq]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 34:23-34:32
    Visibility: public -/
def BloomFilter.Insts.CoreCmpPartialEqBloomFilter.eq
  (self : BloomFilter) (other : BloomFilter) : Result Bool := do
  if self.nbits = other.nbits
  then
    if self.k = other.k
    then
      alloc.vec.partial_eq.PartialEqVec.eq core.cmp.PartialEqU8 self.bits
        other.bits
    else ok false
  else ok false

/-- Trait implementation: [pedra_aeneas_bloom_kernel::{impl core::cmp::PartialEq<pedra_aeneas_bloom_kernel::BloomFilter> for pedra_aeneas_bloom_kernel::BloomFilter}]
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 34:23-34:32 -/
@[reducible]
def BloomFilter.Insts.CoreCmpPartialEqBloomFilter : core.cmp.PartialEq
  BloomFilter BloomFilter := {
  eq := BloomFilter.Insts.CoreCmpPartialEqBloomFilter.eq
}

/-- [pedra_aeneas_bloom_kernel::{impl core::cmp::Eq for pedra_aeneas_bloom_kernel::BloomFilter}::assert_fields_are_eq]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 34:34-34:36
    Visibility: public -/
def BloomFilter.Insts.CoreCmpEq.assert_fields_are_eq
  (self : BloomFilter) : Result Unit := do
  ok ()

/-- Trait implementation: [pedra_aeneas_bloom_kernel::{impl core::cmp::Eq for pedra_aeneas_bloom_kernel::BloomFilter}]
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 34:34-34:36 -/
@[reducible]
def BloomFilter.Insts.CoreCmpEq : core.cmp.Eq BloomFilter := {
  partialEqInst := BloomFilter.Insts.CoreCmpPartialEqBloomFilter
  assert_fields_are_eq := BloomFilter.Insts.CoreCmpEq.assert_fields_are_eq
}

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::always_true]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 46:4-52:5
    Visibility: public -/
def BloomFilter.always_true : Result BloomFilter := do
  ok { bits := (alloc.vec.Vec.new Std.U8), nbits := 0#u32, k := 0#u32 }

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::is_active]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 56:4-58:5
    Visibility: public -/
def BloomFilter.is_active (self : BloomFilter) : Result Bool := do
  if self.nbits > 0#u32
  then
    if self.k > 0#u32
    then let b ← alloc.vec.Vec.is_empty Global self.bits
         ok (¬ b)
    else ok false
  else ok false

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::with_capacity]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 62:4-79:5
    Visibility: public -/
def BloomFilter.with_capacity
  (n_keys : Std.Usize) (bits_per_key : Std.Usize) : Result BloomFilter := do
  if n_keys = 0#usize
  then BloomFilter.always_true
  else
    if bits_per_key = 0#usize
    then BloomFilter.always_true
    else
      let i ← core.num.Usize.saturating_mul n_keys bits_per_key
      let raw1 ← core.cmp.Ord.max.default core.cmp.OrdUsize i 64#usize
      let i1 ← lift (UScalar.cast .Usize core.num.U32.MAX)
      let i2 ← core.cmp.Ord.min.trait_default core.cmp.OrdUsize raw1 i1
      let r ← core.convert.num.ptr_try_from_impls.TryFromU32Usize.try_from i2
      let nbits ← core.result.Result.unwrap_or r core.num.U32.MAX
      let i3 ← bits_per_key * 69#usize
      let i4 ← i3 / 100#usize
      let r1 ←
        core.convert.num.ptr_try_from_impls.TryFromU32Usize.try_from i4
      let i5 ← core.result.Result.unwrap_or r1 1#u32
      let k ← core.cmp.impls.OrdU32.clamp i5 1#u32 30#u32
      let i6 ← lift (UScalar.cast .Usize nbits)
      let nbytes ← core.num.Usize.div_ceil i6 8#usize
      let v ← alloc.vec.from_elem core.clone.CloneU8 0#u8 nbytes
      ok { bits := v, nbits, k }

/-- [pedra_aeneas_bloom_kernel::fnv1a64_seed]: loop body 0:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 212:4-215:5 -/
@[rust_loop_body]
def fnv1a64_seed_loop.body
  (iter : core.slice.iter.Iter Std.U8) (hash : Std.U64) :
  Result (ControlFlow ((core.slice.iter.Iter Std.U8) × Std.U64) Std.U64)
  := do
  let (o, iter1) ← core.slice.iter.IteratorSliceIter.next iter
  match o with
  | none => ok (done hash)
  | some b =>
    let i ← lift (core.convert.num.FromU64U8.from b)
    let hash1 ← lift (hash ^^^ i)
    let hash2 ← lift (core.num.U64.wrapping_mul hash1 1099511628211#u64)
    ok (cont (iter1, hash2))

/-- [pedra_aeneas_bloom_kernel::fnv1a64_seed]: loop 0:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 212:4-215:5 -/
@[rust_loop]
def fnv1a64_seed_loop
  (iter : core.slice.iter.Iter Std.U8) (hash : Std.U64) : Result Std.U64 := do
  loop
    (fun (iter1, hash1) => fnv1a64_seed_loop.body iter1 hash1)
    (iter, hash)

/-- [pedra_aeneas_bloom_kernel::fnv1a64_seed]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 210:0-217:1 -/
def fnv1a64_seed (data : Slice Std.U8) (seed : Std.U64) : Result Std.U64 := do
  let iter ←
    SharedSlice.Insts.CoreIterTraitsCollectIntoIteratorSharedIter.into_iter
      data
  fnv1a64_seed_loop iter seed

/-- [pedra_aeneas_bloom_kernel::fnv1a64]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 206:0-208:1 -/
def fnv1a64 (data : Slice Std.U8) : Result Std.U64 := do
  fnv1a64_seed data 14695981039346656037#u64

/-- [pedra_aeneas_bloom_kernel::hash_pair]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 196:0-204:1 -/
def hash_pair (key : Slice Std.U8) : Result (Std.U64 × Std.U64) := do
  let h1 ← fnv1a64 key
  let h2 ← fnv1a64_seed key 11400714819323198485#u64
  if h2 = 0#u64
  then ok (h1, 11400714819323198485#u64)
  else ok (h1, h2)

/-- [pedra_aeneas_bloom_kernel::set_bit]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 187:0-189:1 -/
def set_bit (bits : Slice Std.U8) (i : Std.Usize) : Result (Slice Std.U8) := do
  let i1 ← i % 8#usize
  let i2 ← 1#u8 <<< i1
  let i3 ← i / 8#usize
  let i4 ← Slice.index_usize bits i3
  let i5 ← lift (i4 ||| i2)
  Slice.update bits i3 i5

/-- [pedra_aeneas_bloom_kernel::bit_index]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 176:0-185:1 -/
def bit_index (bit : Std.U64) : Result Std.Usize := do
  let i ← lift (core.convert.num.FromU64U32.from core.num.U32.MAX)
  if bit > i
  then ok 0#usize
  else let i1 ← lift (UScalar.cast .U32 bit)
       ok (UScalar.cast .Usize i1)

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::insert]: loop body 0:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 88:8-91:9
    Visibility: public -/
@[rust_loop_body]
def BloomFilter.insert_loop.body
  (h1 : Std.U64) (h2 : Std.U64) (nbits : Std.U64)
  (iter : core.ops.range.Range Std.U32) (v : alloc.vec.Vec Std.U8) :
  Result (ControlFlow ((core.ops.range.Range Std.U32) × (alloc.vec.Vec
    Std.U8)) (alloc.vec.Vec Std.U8))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepU32 iter
  match o with
  | none => ok (done v)
  | some i =>
    let i1 ← lift (core.convert.num.FromU64U32.from i)
    let i2 ← lift (core.num.U64.wrapping_mul i1 h2)
    let i3 ← lift (core.num.U64.wrapping_add h1 i2)
    let bit ← i3 % nbits
    let (s, deref_mut_back) ← lift (alloc.vec.Vec.deref_mut v)
    let i4 ← bit_index bit
    let s1 ← set_bit s i4
    let v1 := deref_mut_back s1
    ok (cont (iter1, v1))

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::insert]: loop 0:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 88:8-91:9
    Visibility: public -/
@[rust_loop]
def BloomFilter.insert_loop
  (iter : core.ops.range.Range Std.U32) (v : alloc.vec.Vec Std.U8)
  (h1 : Std.U64) (h2 : Std.U64) (nbits : Std.U64) :
  Result (alloc.vec.Vec Std.U8)
  := do
  loop
    (fun (iter1, v1) => BloomFilter.insert_loop.body h1 h2 nbits iter1 v1)
    (iter, v)

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::insert]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 82:4-92:5
    Visibility: public -/
def BloomFilter.insert
  (self : BloomFilter) (key : Slice Std.U8) : Result BloomFilter := do
  let b ← BloomFilter.is_active self
  if b
  then
    let (h1, h2) ← hash_pair key
    let nbits ← lift (core.convert.num.FromU64U32.from self.nbits)
    let v ←
      BloomFilter.insert_loop { start := 0#u32, «end» := self.k } self.bits
        h1 h2 nbits
    ok { self with bits := v }
  else ok self

/-- [pedra_aeneas_bloom_kernel::test_bit]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 191:0-193:1 -/
def test_bit (bits : Slice Std.U8) (i : Std.Usize) : Result Bool := do
  let i1 ← i / 8#usize
  let i2 ← Slice.index_usize bits i1
  let i3 ← i % 8#usize
  let i4 ← 1#u8 <<< i3
  let i5 ← lift (i2 &&& i4)
  ok (i5 != 0#u8)

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::may_contain]: loop body 0:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 102:8-109:5
    Visibility: public -/
@[rust_loop_body]
def BloomFilter.may_contain_loop.body
  (v : alloc.vec.Vec Std.U8) (h1 : Std.U64) (h2 : Std.U64) (nbits : Std.U64)
  (iter : core.ops.range.Range Std.U32) :
  Result (ControlFlow (core.ops.range.Range Std.U32) Bool)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepU32 iter
  match o with
  | none => ok (done true)
  | some i =>
    let i1 ← lift (core.convert.num.FromU64U32.from i)
    let i2 ← lift (core.num.U64.wrapping_mul i1 h2)
    let i3 ← lift (core.num.U64.wrapping_add h1 i2)
    let bit ← i3 % nbits
    let s := alloc.vec.Vec.deref v
    let i4 ← bit_index bit
    let b ← test_bit s i4
    if b
    then ok (cont iter1)
    else ok (done false)

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::may_contain]: loop 0:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 102:8-109:5
    Visibility: public -/
@[rust_loop]
def BloomFilter.may_contain_loop
  (iter : core.ops.range.Range Std.U32) (v : alloc.vec.Vec Std.U8)
  (h1 : Std.U64) (h2 : Std.U64) (nbits : Std.U64) :
  Result Bool
  := do
  loop
    (fun iter1 => BloomFilter.may_contain_loop.body v h1 h2 nbits iter1)
    iter

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::may_contain]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 96:4-109:5
    Visibility: public -/
def BloomFilter.may_contain
  (self : BloomFilter) (key : Slice Std.U8) : Result Bool := do
  let b ← BloomFilter.is_active self
  if b
  then
    let (h1, h2) ← hash_pair key
    let nbits ← lift (core.convert.num.FromU64U32.from self.nbits)
    BloomFilter.may_contain_loop { start := 0#u32, «end» := self.k }
      self.bits h1 h2 nbits
  else ok true

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::encode]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 113:4-121:5
    Visibility: public -/
def BloomFilter.encode
  (self : BloomFilter) : Result (alloc.vec.Vec Std.U8) := do
  let i := alloc.vec.Vec.len self.bits
  let i1 ← 12#usize + i
  let out := alloc.vec.Vec.with_capacity Std.U8 i1
  let a ← lift (core.num.U32.to_le_bytes self.nbits)
  let s ← lift (Array.to_slice a)
  let out1 ← alloc.vec.Vec.extend_from_slice core.clone.CloneU8 out s
  let a1 ← lift (core.num.U32.to_le_bytes self.k)
  let s1 ← lift (Array.to_slice a1)
  let out2 ← alloc.vec.Vec.extend_from_slice core.clone.CloneU8 out1 s1
  let i2 := alloc.vec.Vec.len self.bits
  let r ← core.convert.num.ptr_try_from_impls.TryFromU32Usize.try_from i2
  let nbytes ← core.result.Result.unwrap_or r core.num.U32.MAX
  let a2 ← lift (core.num.U32.to_le_bytes nbytes)
  let s2 ← lift (Array.to_slice a2)
  let out3 ← alloc.vec.Vec.extend_from_slice core.clone.CloneU8 out2 s2
  let s3 := alloc.vec.Vec.deref self.bits
  alloc.vec.Vec.extend_from_slice core.clone.CloneU8 out3 s3

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::decode]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 128:4-154:5
    Visibility: public -/
def BloomFilter.decode
  (buf : Slice Std.U8) : Result (core.result.Result BloomFilter String) := do
  let b ← core.slice.Slice.is_empty buf
  if b
  then let bf ← BloomFilter.always_true
       ok (core.result.Result.Ok bf)
  else
    let i := Slice.len buf
    if i < 12#usize
    then
      let args := Slice.len buf
      let a ←
        core.fmt.rt.Argument.new_display Usize.Insts.CoreFmtDisplay args
      let a1 ←
        core.fmt.Arguments.new
          (Array.make 27#usize [
            17#u8, 98#u8, 108#u8, 111#u8, 111#u8, 109#u8, 32#u8, 116#u8,
            111#u8, 111#u8, 32#u8, 115#u8, 104#u8, 111#u8, 114#u8, 116#u8,
            58#u8, 32#u8, 192#u8, 6#u8, 32#u8, 98#u8, 121#u8, 116#u8, 101#u8,
            115#u8, 0#u8
            ]) (Array.make 1#usize [ a ])
      let s ← alloc.fmt.format a1
      let s1 ← core.hint.must_use s
      ok (core.result.Result.Err s1)
    else
      let i1 ← Slice.index_usize buf 0#usize
      let i2 ← Slice.index_usize buf 1#usize
      let i3 ← Slice.index_usize buf 2#usize
      let i4 ← Slice.index_usize buf 3#usize
      let nbits ←
        lift (core.num.U32.from_le_bytes
          (Array.make 4#usize [ i1, i2, i3, i4 ]))
      let i5 ← Slice.index_usize buf 4#usize
      let i6 ← Slice.index_usize buf 5#usize
      let i7 ← Slice.index_usize buf 6#usize
      let i8 ← Slice.index_usize buf 7#usize
      let k ←
        lift (core.num.U32.from_le_bytes
          (Array.make 4#usize [ i5, i6, i7, i8 ]))
      let i9 ← Slice.index_usize buf 8#usize
      let i10 ← Slice.index_usize buf 9#usize
      let i11 ← Slice.index_usize buf 10#usize
      let i12 ← Slice.index_usize buf 11#usize
      let nbytes ←
        lift (core.num.U32.from_le_bytes
          (Array.make 4#usize [ i9, i10, i11, i12 ]))
      let nbytes_us ← lift (UScalar.cast .Usize nbytes)
      let i13 ← 12#usize + nbytes_us
      let i14 := Slice.len buf
      if i13 > i14
      then
        let s ←
          core.convert.IntoFrom.into
            alloc.string.String.Insts.CoreConvertFromShared0Str (toStr
            "bloom bits truncated")
        ok (core.result.Result.Err s)
      else
        if nbits = 0#u32
        then let bf ← BloomFilter.always_true
             ok (core.result.Result.Ok bf)
        else
          if k = 0#u32
          then let bf ← BloomFilter.always_true
               ok (core.result.Result.Ok bf)
          else
            if nbytes = 0#u32
            then
              let bf ← BloomFilter.always_true
              ok (core.result.Result.Ok bf)
            else
              let i15 := Slice.len buf
              let i16 ← i15 - 12#usize
              let residual ← lift (UScalar.cast .U64 i16)
              let b1 ← bloom_header_ok nbits k nbytes residual
              if b1
              then
                let s ←
                  core.slice.index.Slice.index
                    (core.slice.index.SliceIndexRangeUsizeSlice Std.U8) buf
                    { start := 12#usize, «end» := i13 }
                let bits ← alloc.slice.Slice.to_vec core.clone.CloneU8 s
                ok (core.result.Result.Ok { bits, nbits, k })
              else
                let a ←
                  core.fmt.rt.Argument.new_display U32.Insts.CoreFmtDisplay
                    nbits
                let a1 ←
                  core.fmt.rt.Argument.new_display U32.Insts.CoreFmtDisplay k
                let a2 ←
                  core.fmt.rt.Argument.new_display U32.Insts.CoreFmtDisplay
                    nbytes
                let a3 ←
                  core.fmt.rt.Argument.new_display U32.Insts.CoreFmtDisplay
                    MAX_K
                let a4 ←
                  core.fmt.Arguments.new
                    (Array.make 58#usize [
                      28#u8, 98#u8, 108#u8, 111#u8, 111#u8, 109#u8, 32#u8,
                      104#u8, 101#u8, 97#u8, 100#u8, 101#u8, 114#u8, 32#u8,
                      105#u8, 110#u8, 118#u8, 97#u8, 108#u8, 105#u8, 100#u8,
                      58#u8, 32#u8, 110#u8, 98#u8, 105#u8, 116#u8, 115#u8,
                      32#u8, 192#u8, 3#u8, 32#u8, 107#u8, 32#u8, 192#u8, 8#u8,
                      32#u8, 110#u8, 98#u8, 121#u8, 116#u8, 101#u8, 115#u8,
                      32#u8, 192#u8, 8#u8, 32#u8, 40#u8, 109#u8, 97#u8, 120#u8,
                      32#u8, 107#u8, 32#u8, 192#u8, 1#u8, 41#u8, 0#u8
                      ]) (Array.make 4#usize [ a, a1, a2, a3 ])
                let s ← alloc.fmt.format a4
                let s1 ← core.hint.must_use s
                ok (core.result.Result.Err s1)

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::bit_count]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 158:4-160:5
    Visibility: public -/
def BloomFilter.bit_count (self : BloomFilter) : Result Std.U32 := do
  ok self.nbits

/-- [pedra_aeneas_bloom_kernel::{pedra_aeneas_bloom_kernel::BloomFilter}::hash_count]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 164:4-166:5
    Visibility: public -/
def BloomFilter.hash_count (self : BloomFilter) : Result Std.U32 := do
  ok self.k

/-- [pedra_aeneas_bloom_kernel::may_contain_mut_extra_probe]: loop body 0:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 230:4-237:1
    Visibility: public -/
@[rust_loop_body]
def may_contain_mut_extra_probe_loop.body
  (v : alloc.vec.Vec Std.U8) (h1 : Std.U64) (h2 : Std.U64) (nbits : Std.U64)
  (iter : core.ops.range.RangeInclusive Std.U32) :
  Result (ControlFlow (core.ops.range.RangeInclusive Std.U32) Bool)
  := do
  let (o, iter1) ←
    core.ops.range.RangeInclusive.Insts.CoreIterTraitsIteratorIterator.next
      core.iter.range.StepU32 iter
  match o with
  | none => ok (done true)
  | some i =>
    let i1 ← lift (core.convert.num.FromU64U32.from i)
    let i2 ← lift (core.num.U64.wrapping_mul i1 h2)
    let i3 ← lift (core.num.U64.wrapping_add h1 i2)
    let bit ← i3 % nbits
    let s := alloc.vec.Vec.deref v
    let i4 ← bit_index bit
    let b ← test_bit s i4
    if b
    then ok (cont iter1)
    else ok (done false)

/-- [pedra_aeneas_bloom_kernel::may_contain_mut_extra_probe]: loop 0:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 230:4-237:1
    Visibility: public -/
@[rust_loop]
def may_contain_mut_extra_probe_loop
  (iter : core.ops.range.RangeInclusive Std.U32) (v : alloc.vec.Vec Std.U8)
  (h1 : Std.U64) (h2 : Std.U64) (nbits : Std.U64) :
  Result Bool
  := do
  loop
    (fun iter1 => may_contain_mut_extra_probe_loop.body v h1 h2 nbits iter1)
    iter

/-- [pedra_aeneas_bloom_kernel::may_contain_mut_extra_probe]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 224:0-237:1
    Visibility: public -/
def may_contain_mut_extra_probe
  (f : BloomFilter) (key : Slice Std.U8) : Result Bool := do
  let b ← BloomFilter.is_active f
  if b
  then
    let (h1, h2) ← hash_pair key
    let nbits ← lift (core.convert.num.FromU64U32.from f.nbits)
    let iter ← core.ops.range.RangeInclusive.new 0#u32 f.k
    may_contain_mut_extra_probe_loop iter f.bits h1 h2 nbits
  else ok true

/-- [pedra_aeneas_bloom_kernel::may_contain_mut_hash_mismatch]: loop body 0:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 250:4-257:1
    Visibility: public -/
@[rust_loop_body]
def may_contain_mut_hash_mismatch_loop.body
  (v : alloc.vec.Vec Std.U8) (h1 : Std.U64) (h2 : Std.U64) (nbits : Std.U64)
  (iter : core.ops.range.Range Std.U32) :
  Result (ControlFlow (core.ops.range.Range Std.U32) Bool)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepU32 iter
  match o with
  | none => ok (done true)
  | some i =>
    let i1 ← lift (core.convert.num.FromU64U32.from i)
    let i2 ← lift (core.num.U64.wrapping_mul i1 h2)
    let i3 ← lift (core.num.U64.wrapping_add h1 i2)
    let bit ← i3 % nbits
    let s := alloc.vec.Vec.deref v
    let i4 ← bit_index bit
    let b ← test_bit s i4
    if b
    then ok (cont iter1)
    else ok (done false)

/-- [pedra_aeneas_bloom_kernel::may_contain_mut_hash_mismatch]: loop 0:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 250:4-257:1
    Visibility: public -/
@[rust_loop]
def may_contain_mut_hash_mismatch_loop
  (iter : core.ops.range.Range Std.U32) (v : alloc.vec.Vec Std.U8)
  (h1 : Std.U64) (h2 : Std.U64) (nbits : Std.U64) :
  Result Bool
  := do
  loop
    (fun iter1 => may_contain_mut_hash_mismatch_loop.body v h1 h2 nbits iter1)
    iter

/-- [pedra_aeneas_bloom_kernel::may_contain_mut_hash_mismatch]:
    Source: '../../../crates/pedradb-core/src/bloom.rs', lines 243:0-257:1
    Visibility: public -/
def may_contain_mut_hash_mismatch
  (f : BloomFilter) (key : Slice Std.U8) : Result Bool := do
  let b ← BloomFilter.is_active f
  if b
  then
    let (h1, h2) ← hash_pair key
    let h21 ← lift (h2 ^^^ 1#u64)
    let nbits ← lift (core.convert.num.FromU64U32.from f.nbits)
    may_contain_mut_hash_mismatch_loop { start := 0#u32, «end» := f.k }
      f.bits h1 h21 nbits
  else ok true

end pedra_aeneas_bloom_kernel
