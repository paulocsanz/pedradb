[package]
name = "pedradb-capi"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Montanha FDB-shaped in-process C ABI (not libfdb_c; store stays forbid(unsafe_code))"
rust-version.workspace = true

[lib]
path = "src/lib_kernel.rs"
name = "pedradb_capi"
crate-type = ["rlib", "cdylib", "staticlib"]

[dependencies]
pedradb-store.workspace = true
