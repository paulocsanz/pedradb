[package]
name = "pedradb-stream"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Durable append-only stream over PedraDB (RFC-0010 P2.4 / JetStream-class need)"


[lib]
path = "src/lib_kernel.rs"
[dependencies]
pedradb-core.workspace = true
pedradb-io-uring.workspace = true
bytes.workspace = true
thiserror.workspace = true

[dev-dependencies]
# Same production cursor_kernel as Stream::peek / ack (F54).
stateright = "0.31"
# Clone-trap test twin_agrees_with_journal_pin_kernel_on_full_domain.
pedradb-journal.workspace = true
