# RFC-0224 P2.4 recusa medida — TV do objeto pinado `write_admission_kernel.rs`

**Date:** 2026-09-14
**HEAD:** `8ad49dc0`
**Axis:** binary / `R-rustc` never_floor
**Reaffirms:** `findings/2026-09-14-rfc0222-p25-translation-validation-recusa.md` (`e7864aaf`) with a new HEAD.
**Parent:** RFC-0172 (idle IR 8/8 vs a boolean spec; residual `R-rustc` remains `never`)

## Measured

`scripts/tv_write_admission.sh` TVs LLVM IR of `write_admission_idle` against `!mem && !pressure && !stall` (8/8). That is **not** a fail-closed correspondence between the rustc object of `write_admission_kernel.rs` and the Lean/Verus term (`WriteAdmissionKernel.lean` / last-wins Verus).

`never_floor` still lists `R-rustc`. `glue.db_rs_extracted` = false. No object-file ↔ Lean extract harness in CI.

## What would close it

A pinned (host, rustc, crate, file) TV run that checks the **object** rustc emits for `write_admission_kernel.rs` against the Lean/Verus term, fail-closed, registered. That still does not prove rustc; it proves one object. `R-rustc` stays `never` either way.

## Residual

P2.4 is research-scale (seL4 paid years after C). Published refusal: no object↔Lean TV artifact. `R-rustc` stays on `never_floor`. Not a silent skip.
