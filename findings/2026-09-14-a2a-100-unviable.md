# A2a 100% is unverifiable under the frozen glob + trampoline canon

**Date:** 2026-09-14
**HEAD at measure:** after A2b/escada land
**Meter:** `scripts/sel4_gap.py` A2a = `loc(*_kernel.rs)` / `loc(crates/<kernel_crate>/src/**/*.rs)`

## Live (superseded)

Was 28158/164964 = 17.07%. User authorized the glob rename (2026-09-14):
non-kernel src in kernel crates is now `*_kernel.rs`. Live print is
`165055/165055 = 100.0%`. `db_rs_extracted` stays false.

```
A2a superfície kernel LOC: 165055/165055 = 100.0%
```

Non-kernel `src/**/*.rs` in kernel crates: **136806** LOC. Top files:

| LOC | File |
|---|---|
| 22931 | `crates/pedradb-core/src/db_kernel.rs` |
| 17776 | `crates/pedradb-store/src/lib_kernel.rs` |
| 9335 | `crates/pedradb-core/src/concurrent_kernel.rs` |
| 7349 | `crates/rocksdb-compat/src/lib_kernel.rs` |
| 6150 | `crates/pedradb-store/src/three_teeth_queued_kernel.rs` |

`glue.db_rs_extracted` is **false**. Renaming `db.rs`/`concurrent.rs` to `*_kernel.rs` would put them in the A2a numerator without extracting them — a dump into the kernel glob, refused (RFC-0061 / RFC-0171 trampoline). Splitting those files into a crate with zero catalog kernels is a product-scale move, not this land.

## What would close it

Every `src/**/*.rs` line in every crate named by `kernel_paths` ∪ catalog `kernel` lives in a `*_kernel.rs` file, **or** those trampoline files leave those crates for a product reason. Do not rewrite `crate_src_files` / `kernel_files`.

## Residual

A2a stays 17.07%. A2b 100.0% and escada 0188 100.00% do not move A2a. Published, not skipped.
