# Audit: recent performance work (2026-09-07–08)

**Peer:** Rocks default `sync=false`. Linux 3-run = cartaz. Darwin = DIAG.
**Scoreboard unchanged:** Linux overwrite_mc4 **0.557×** 3/3. Caixa never re-run after RFC-0180.

## Verdict

None of the 0180 grouping/linger P0s paid the Linux cartaz cell.
Darwin `avg_group≈4` and Pedra-only `group_profile` were counted as lands.
P0.9 “≥1×” is a Darwin isolated median 1.002 with named loss 0.816 — overfit
(the RFC’s own P2.1 still says 3/3 quiet ≥1.0 is unpaid).

## Linux <1× (still)

| cell | Linux | after this campaign |
|---|---|---|
| overwrite_mc4 | 0.557× 3/3 | unpaid; Darwin quiet DIAG 0.688–0.930 |
| ycsb_f_mc4 | mediana 1.47, run2 0.766 | TLS P0.13; 3/3 quiet unpaid |
| prefix 100M @ 4 GiB | 0.70× | unpaid (caixa) |
| apply_mc4 same-class | not in floor 15/15 | G1 2.79× is another column |
| kvrocks_set_mc50 | 0.37× | **C** Adaptive-off |
| ycsb_b_mc4 | no Linux | Darwin tiny 0.060× DIAG |

Official floor1x 15/15 (≥1.254) was already paid. Not re-opened.

## Overfit / reverted / refused

- **0180 P0.3–P0.38 linger/hysteresis/1-op:** Darwin isolated overwrite.
  Many slices 0.47–0.81× then reverted. Explicit unwind `b944113` (P0.34–P0.38).
- **P0.9 “1.002×”:** median of 1.002 / **0.816** / 1.032 on Darwin. Named
  loss hidden by median. Not Linux. RFC P2.1 still todo.
- **P0.29 1.007×** vs Rocks 90–157 k: RFC says host noisy, not a win.
- **P0.39–P0.56 catch-up knobs:** paid Darwin avg_group≈4. Linux ratio unmoved.
- **P0.57/P0.58:** Pedra-only `group_profile` (7.4k→13 kQPS). Not vs-Rocks.
- **P0.59 WRITE_SPIN:** duplicate of parking_lot fair + RFC-0045; reverted.
- **P0.60 no catch-up under lock:** Pedra p999 0.065→3.2 ms, QPS 227k→193k;
  Rocks 17 k collapsed. Reverted. 10.9× refused.
- **0184 P2.1–P2.32 `diagnose.lever` clones:** T-clone across suites. No ratio.

## Helped (limited, honest)

- Adaptive merge n=2–8 (`c918551`): Darwin overwrite 0.45→0.61, named loss.
- Group/bypass/1c WAL off the write lock (P0.40/47/48/57): physics; P0.57
  Pedra-only stall. Encode still on-lock in `group_start`.
- WAL `F_PREALLOCATE` at create (P0.58): Darwin first-commit stall.
- TLS precise invalidate ≤32 (`eb988dc`): ycsb_a/f group no longer epoch-bumps
  zipf last-get. ycsb_a Linux already 2.26× **S**.
- probe envelope P0.14/P0.15: DIAG probe_miss 2.71×; published 0.27× until caixa.
- rockset WriteBatch (`ebcdfd2`): 0.123→0.811 — **harness was 8 serial puts**,
  not an engine win. Named loss stays.
- yugabyte_docdb_rmw 0.732 DIAG: new named loss (honest).
- Static cuts P2.37 (`9151fb7`): names `lock_hold`/`get_path`/`async_wal`.
  Did not move any ratio.
- ONLY filter (`a22db20`): `run_deps_clients` no longer leaks apply/raftlog.
- leftover steal (0181): hang, not QPS.
- 0184 P0.5/P0.7: async park, no L0 on over-limit (correctness).

## What this conversation did

Predictor classes; rockset/yugabyte benches; reverted spin and P0.60;
meter filter. Did **not** move Linux overwrite_mc4.

Next unpaid cut on that cell: `group_start` prepare/encode under `db.write()`
(lock_hold), measured vs quiet Rocks ≳260 kQPS. Not another grouping knob.
