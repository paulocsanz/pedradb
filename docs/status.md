# Status ledger — crates, RFCs, and reproduction commands

Detailed per-crate shipping status and the RFC program. Moved here from the
README front page; this is the living ledger, the README is the pitch.

## Status (shipped)

| Layer | Crates | Status |
|-------|--------|--------|
| Kernel LSM + TX | `pedradb-core` | ✅ WAL, MemTable, **SST v4 + Bloom + lz4**, leveled compact, MANIFEST v2, LOCK, checkpoint/stats/verify, scan, ConcurrentDb, range delete |
| Fault injection | `pedradb-sim` | ✅ FailingEnv, RecordingEnv, lying sync, short-write, seed, Arc |
| Oracle | `pedradb-oracle` | ✅ model (+ optional RocksDB) |
| Ordered apply | `pedradb-apply` | ✅ LogApplier, FakeLog, KvService, InProcessCluster |
| Raft (TCP + persist) | `pedradb-raft`, `pedra-raft-node` | ✅ elect, put, multi-process, failover |
| **Montanha-Store** multi-Raft | `pedradb-store`, `montanha-tcp` | ✅ ranges + put/get + DCS + Queued RPC + **TCP multi-host (MTCP)** |
| WAL ship replica | `pedradb-replicate` | ✅ |
| DCS SM | `pedradb-dcs` | ✅ CAS, lease, watch, leader lock |
| HTTP wire | `pedradb-http` | ✅ KV + DCS |
| SQL subset | `pedradb-sql` | ✅ CREATE/INSERT/SELECT/DELETE (not PG wire) |
| Durable stream | `pedradb-stream` | ✅ publish + consumer cursor |
| DST seed sweep | `pedradb-dst` | ✅ |
| **io_uring Env** | `pedradb-io-uring` | ✅ Linux write+fsync; POSIX fallback elsewhere |
| **Ops (backup/PITR/migrate)** | `pedradb-ops`, `pedra` CLI | ✅ base backup, ship_wal, restore, PITR, inspect, migrate |
| Toward Rocks/Pebble/Redwood | [RFC-0014](rfc/0014-rocks-pebble-redwood-maturity.md) | done P0–P2 (OCC, vlog spill, incremental backup) |
| Audit durability / Env fixes | [RFC-0015](rfc/0015-audit-pedradb-correctness-fixes.md) | done (P0–P2) |
| Pedra production robustness | [RFC-0016](rfc/0016-pedradb-production-robustness.md) | P0 done — `compact_vlog`, stats, soak; P1 group-commit done; P1.4/P2.1 open |
| Montanha FDB-class substrate | [RFC-0017](rfc/0017-montanha-fdb-class-substrate.md) | P0 done (incl. **TCP multi-host P0.1** + caixote Linux lab); P2 open |
| L1 primitive for platform + Scylla-need | [RFC-0019](rfc/0019-local-primitive-for-platform-and-scylla-need.md) | done (P0–P2.2) — CAS, seq pin, change feed, multi_get, soak, compact_for_reads |
| Synthetic field maturity | [RFC-0020](rfc/0020-synthetic-field-maturity.md) | **P0–P2 done** — gate, soaks, canaries (lease/index/journal), cluster matrix, explore, race, fuzz, residuals |
| World in-tree (FDB-shaped determinism) | [RFC-0050](rfc/0050-world-in-tree-fdb-determinism.md) | **done (P0–P2)** — `crates/pedradb-world`: seed→`trace_hash` + invariantes + seam inventory no CI; lab=produto (PeerMsg, canaries, buggify); P2: π ordena `World::run` (5º seam), swarm L28 24/24 clean (gate REAL aberto), papel fold no mesmo seed, det_io hard-CT com sibling. Not FDB Simulation |
| Beyond Sim2 holes (π / layers / OS) | [RFC-0051](rfc/0051-beyond-fdb-sim-holes.md) | **done (P0–P2)** — PCT sobre `ConcurrentDb` real: plantado d=2 3/256 (seq 0), fence de grupo no fsync off-lock 9/256 (seq 0), OCC plantado 38/256 cross-group / correto 0/256 (oráculo group-aware), CommitUnknown canário, trial `FailingEnvArc<IoUringEnv>` Linux, guards spawn/wall-clock no CI. Not “more trusted than FDB” |
| Intensidade máxima (paralelo + caixas + formal) | [RFC-0057](rfc/0057-maximum-intensity-parallel-dst-boxes-formal.md) | **draft (P0.1+P0.2 done)** — swarm DST multi-núcleo com forenses por-run e trials paralelos não-interferentes; caixas (Miri/TSan/ASan) como jobs irmãos no CI; kernel formal do group-commit + OCC. “100%” = espaço verificável relativo ao TCB; residual publicado |
| Modo verificado (fallback dos kernels) | [RFC-0058](rfc/0058-verified-mode-kernel-derived-fallback.md) | **P0+P1+P2 done** — perfil de produto com seções críticas = kernels provados (`OpenOptions::verified()`; group-commit de volta por teorema: `group_commit_kernel.rs`, Verus 14/0 + Lean no-sorry, merge ativo com catch-up 0 e bypass async — `verified-v2`; `profile_report()` amarrado ao `catalog.json` machine-checked); suíte FailingEnv + World + PCT rodando no perfil (silent_wrong=0, fence ≤ 1 escritor); `open_verified` em fold/lease/dcs/compat (StdEnv por tipo); derivação de semântica verified=full nos oráculos; CI `verified-mode`; ring io_uring **fora** do modo com contrato publicado (P2.2); `PEDRA_VERIFIED=1` no CLI como linha de produto (P2.3). Extração total segue REFUSE (VeriBetrKV 8×) |
| Escala massiva paralela + invariantes de cluster | [RFC-0059](rfc/0059-massive-scale-parallel-dst-and-cluster-invariants.md) | **draft (P0 done)** — `world_swarm` (work-stealing por núcleo, backend mem com mesmas seams de falha, gate serial-vs-paralelo por `trace_hash`); invariant checker cross-node no estado convergido (autenticidade/split-brain/ressurreição); escala 7/9 nós; 4 F-found corrigidos com regressão pinada (CRC do frame, discard escapado, CommitUnknown por payload, snapshot stale wipe). Campanhas: 16384@3n/4096@7n/4096@9n-4ranges — sem claim CPU-hours vs FDB |
| DST inside boxes (Miri / ASan / TCG) | [RFC-0052](rfc/0052-dst-inside-boxes.md) | **draft** — compose in the cycle, not one VM. P0: `FailingEnv` under Miri. REFUSE Miri-in-TCG and TCG benches |
| IronFleet-scale formal (years) | [RFC-0053](rfc/0053-ironfleet-years.md) | **done (Y1–Y3)** — Lean AE/commit extracts; AE/apply caller refinements; reopen kernel + lemmas; bounded liveness sob axioma quórum-vivo; π/VerusSync não disparado (RFC-0051 draft) |
| Entregar o 100% (relativo ao TCB) | [RFC-0056](rfc/0056-one-hundred-percent-delivery.md) | **done** — itens 1–6/8/9/11 do checklist verdes; 7 gated (RFC-0051 PCT); 10 “TCB à vista” (freeze no CI); 12 contínuo. Estado: [one-hundred-percent-report](formal/one-hundred-percent-report.md) |
| Lease / index / journal canaries | `pedradb-lease`, `pedradb-index`, `pedradb-journal` | ✅ W1–W4 workloads silent_wrong=0 |
| Slipstream scale >2× Rocks default (28 cells, 1M–100M) | [RFC-0160](rfc/0160-slipstream-scale-2x.md) | **in-progress** — >2.0 is **not** the 0161 stop. P1.6 bulk SST real bloom in-tree (`probe_miss` is a required miss-path cell, not dropped). P0.5 cap in-tree; v73b lookup tax numbered. Peer Rocks `sync=false` |
| Slipstream scale **>1.5× then stop** (same hydrate/read matrix; `probe_miss` now required) | [RFC-0161](rfc/0161-slipstream-scale-1p5x.md) | **in-progress (slices closed as authorized ceilings)** — **9 PASS / 15 ceiling** on the 24 hydrate/read cells. 100M 3-run v78 (2026-09-04): hydrate **1.23×** (123.7 vs 152.7 s; P2.1 1.18×), settle **88×**, get_hit **1.80×**, get_loop **1.68×**, multi_get **1.74×**; prefix **1.00× tie** (run2 0.75×); **`probe_miss` 0.29× named loss** (2.3–2.4 µs vs 651–692 ns; always-true bulk bloom, now fixed — re-run before a new ratio). 1-run v76 2.40/2.89/2.67 not kept (Rocks point-reads got faster). Ceiling K2 vs 1.5× no hydrate. 25M get_loop not published (Rocks OOB). Stop; not 2.0. `findings/2026-09-04-100m-readlegs-3run/` |
| Hydrate **>1× em todas as escalas** (degradação de taxa 0.87→0.79 M/s) | [RFC-0162](rfc/0162-hydrate-scale-rate-decay.md) | **done** — P0+P1 fechados (H1 confirmado por intervenção; −2.9 s @100M medidos); P2.1 oficial 3-run (timing OFF, 4/4 exit/run): 25M **1.024×** (29.5 vs 30.2 s), 100M **1.179×** (123.9 vs 146.1 s) — >1× nas duas escalas, 6/6 pares intra-run; células dobradas no gate 0161 (seguem ceiling K2 p/ 1.5×). Aviso: flip 100M majoritariamente Rocks-side (129.0–149.5 vs 117.2–121.2 v76) → 0163 P2.2. `findings/2026-09-03-rfc0162-p21-official/` |
| Anti-overfit: **mais tipos de benchmark + A/B por constante + auditoria de código** (win-ou-ceiling por classe) | [RFC-0163](rfc/0163-anti-overfit-benchmark-breadth.md) | **in-progress (P0+P1.1+P1.2/P1.2b done; P1.3 em curso)** — inventário: 15 linhas (por linha: R1:8 — 4 "leve", 1 bench-side; R2:1; R3–R7:0 linhas), knobs classificados incl. `ROCKS_PARITY_SEED_ASYNC` e `ROCKS_PARITY_CLIENTS_LADDER` (bench-side), span p149b+f208; `p999_ms` no JSON da harness (P1.1); Grid B: **célula mixed RW `ycsb_a_mc4` 25M same-class (decisão do autor): PASS 2,262×** (82.971 vs 36.681 qps; intra-run 3/3 ≥1×); `ycsb_f_mc4` 1,473× (run2 intra-run 0,766× registrada); **perda honesta `deps_cache_overwrite_mc4` 0,557×** (3/3 <1×) → P1.4 nomeia o mecanismo antes de veredito; perna G1 cancelada após run1 4/4 exits 0 — ceiling preliminar c/ mecanismo (`ycsb_a_mc4` 0,063×; barreira fdatasync por op em RMW; incidente v79 run1: OOM bench-side exit 137, corrigido com keyspace flat, engine v78 intacto); qps do Rocks cai monotônica entre runs enquanto compat fica estável (veredito imune por pareamento intra-run) → P2.2; `findings/2026-09-03-rfc0163-p12-mixed-rw/`; P1.4 harness ladder pronta (24/24, smoke mc4+mc16); P1.3 value-size ladder 3-run em curso (v81, engine v78, knob gateado); a seguir concorrência 1–64, recovery pós-SIGKILL — veredito obrigatório por célula (≥1× vs Rocks default 3-run OU ceiling nomeado); skill `audit-overfit` |
| Endurecimento dos achados adversariais (kernel/disco/cluster/bench não-G1) | [RFC-0167](rfc/0167-endurecimento-achados-adversariais.md) | **in-progress (P0+P1+P2 slices gated)** — gates nomeados verdes. Demo×2 idêntico. clippy default core+store verde; `-D warnings` bloqueado por pedantic pré-existente. Workspace test **não** fecha: ENOSPC neste host (disco 97–100%). `manifest::decode` v6 no scrub. P1.6 2× e guest hydrate A/B **não** re-medidos. G1 fora de escopo. `findings/2026-09-06-rfc0167-implementacao/` |
| Telemetria permanente — medir tudo desligado por padrão + padrões de lentidão globais | [RFC-0169](rfc/0169-telemetria-permanente-padroes-lentidao.md) | **implemented (P0–P2)** — crate `pedradb-telemetry` (latch 3-estados, default off, 1 load relaxado/sonda); dump JSON `p50/p95/p99/p999`; `pedra stats --telemetry`; lint `scripts/check_no_ungated_prints.py` no CI; cobertura `every_registered_probe_fires_under_its_op_class` (EDGE explícito p/ uring/EINTR/raft); `scripts/telemetry_diff.py` + ledger `--drift`; padrões nomeados nunca silenciosos. Ligar: `PEDRA_TELEMETRY=1` (ou `write,read,io,cluster`). |
| Refinamento à classe seL4 (close + extract; TCB inalterado) | [RFC-0170](rfc/0170-refinamento-sel4-class.md) | **in-progress (P0–P2 slices done; P2.4 named inventory empty)** — atom=0; `compact_family_key` saiu de `db.rs`. never_floor inalterado; `db_rs_extracted=false`. Não somos seL4. `findings/2026-09-06-rfc0170-db-rs-ifs/` |
| Pagar o preço seL4 (artefacto que corre = termo da prova) | [RFC-0171](rfc/0171-pagar-o-preco-sel4.md) | **done (P0–P2)** — artefacto único `write_admission_kernel.rs` (Verus no path que liga; twin=kernel). put-Ok + recover sem `if` de destino de dados em `db.rs`. Aeneas SOURCE pinado. Trampoline Env inventariado. Filho [0172](rfc/0172-rustc-translation-validation-one-target.md) (TV num alvo; `R-rustc` never). `db_rs_extracted=false`. Terceiro ficheiro de produção neste eixo: `iter_kernel.rs` (`iter_window`). Quarto: `tx_glue_kernel.rs` (`tx_glue`). Quinto: `apply_kernel.rs` (`apply_step`). Sexto: store `compact_kernel.rs` (`compact_unleft`). Sétimo: `commit_kernel.rs` (`commit_raft`). Oitavo: dcs `lease_kernel.rs` (`lease`). Nono: wal `reopen_kernel.rs` (`reopen_outcome`). Décimo: compat `locktab.rs` (`wait_for_deadlock`). Décimo-primeiro: `cf_kernel.rs` (`cf_family`). Décimo-segundo: `vlog_gc_kernel.rs` (`vlog_recover`). Décimo-terceiro: `manifest_kernel.rs` (`manifest_recover`). Décimo-quarto: `ae_kernel.rs` (`ae_entry`). Décimo-quinto: `ae_kernel.rs` (`ae_ack`). Décimo-sexto: store `txn_kernel.rs` (`txn`). Décimo-sétimo: `txn_kernel.rs` (`revert_clears_status`). Décimo-oitavo: `txn_kernel.rs` (`revert_user_action`). Décimo-nono: `txn_kernel.rs` (`should_repair_si_hist`). Vigésimo: `txn_kernel.rs` (`discard_cut`). Vigésimo-primeiro: `txn_kernel.rs` (`leftover_txn_is_aborted`). Vigésimo-segundo: `txn_kernel.rs` (`next_txn_id_after`). Vigésimo-terceiro: `txn_kernel.rs` (`recover_si_generation`). Vigésimo-quarto: `txn_kernel.rs` (`prepare_error_aborts_earlier`). Vigésimo-quinto: `txn_kernel.rs` (`reserve_si_gen`). Vigésimo-sexto: `txn_kernel.rs` (`unreserve_si_gen`). Vigésimo-sétimo: wal `reopen_kernel.rs` (`dictionary_link`). Vigésimo-oitavo: `vlog_gc_kernel.rs` (`blob_gc_pick`). Vigésimo-nono: `leveling.rs` (`leveling`). Trigésimo: `leveling.rs` (`leveling_pick`). Trigésimo-primeiro: `leveling.rs` (`leveling_pushdown`). Trigésimo-segundo: `batch.rs` (`write_record_count`). Trigésimo-terceiro: `merge.rs` (`visible_at`). Trigésimo-quarto: `vote_kernel.rs` (`durable_term`). Trigésimo-quinto: `vote_kernel.rs` (`grant_persist`). Trigésimo-sexto: `vote_kernel.rs` (`vote`). Trigésimo-sétimo: `compact_kernel.rs` (`compact_decision`). Trigésimo-oitavo: `compact_kernel.rs` (`compact_retention`). Trigésimo-nono: `compact_kernel.rs` (`pin_gc`). Quadragésimo: `wal/recover_kernel.rs` (`wal_recover`). Quadragésimo-primeiro: `l28.rs` (`l28_tcp_abort`). Quadragésimo-segundo: `l28.rs` (`l28_tcp_apply`). Quadragésimo-terceiro: `l28.rs` (`l28_tcp_napply`). Quadragésimo-quarto: `l28.rs` (`l28_tcp_clear`). Quadragésimo-quinto: `l28.rs` (`l28_tcp_left`). Quadragésimo-sexto: `l28.rs` (`l28_tcp_hw`). `l28.rs` (`l28_tcp_part`). `l28.rs` (`l28_tcp_trunc`). `l28.rs` (`l28_tcp_odrop`). `l28.rs` (`l28_tcp_nowms`). `l28.rs` (`l28_tcp_dterm`). `l28.rs` (`l28_tcp_hist`). `l28.rs` (`l28_tcp_fence`). `l28.rs` (`l28_tcp_pre`). `l28.rs` (`l28_tcp_peer`). `l28.rs` (`l28_tcp_lid`). `l28.rs` (`l28_tcp_rdr`). `l28.rs` (`l28_tcp_dsc`). `l28.rs` (`l28_tcp_pld`). `l28.rs` (`l28_tcp_std`). `l28.rs` (`l28_tcp_hnt`). `l28.rs` (`l28_tcp_slot`). `l28.rs` (`l28_tcp_sth`). `l28.rs` (`l28_tcp_pj`). `membership_kernel.rs` (`discard_leader`). `membership_kernel.rs` (`discard_uncommitted`; `single_artifact` 82→83). `membership_kernel.rs` (`joint_election`; `single_artifact` 83→84). `membership_kernel.rs` (`joint_leave`; `single_artifact` 84→85). `membership_kernel.rs` (`pending_joint_node`; `single_artifact` 85→86). |
| Translation validation rustc→objeto (um alvo pinado) | [RFC-0172](rfc/0172-rustc-translation-validation-one-target.md) | **done (P0–P2)** — pin aarch64-apple-darwin / rustc 1.97.1 / LLVM 22.1.6. Dump LLVM IR de `write_admission_kernel.rs`. Símbolos idle+admit no objeto. SOURCE.tv sha256-bind. TV: IR de `write_admission_idle` 8/8 = spec. `R-rustc` never. `findings/2026-09-06-rfc0172-tv/` |
| Destino de dados que corre = termo da prova (resto da conta seL4) | [RFC-0174](rfc/0174-destino-de-dados-que-corre-e-o-termo.md) | **done (P0–P2)** — não autoriza “somos seL4”. `flush_kernel.rs` é o termo (Verus 21/0). get/lookup sem `if` cru (`lookup_kernel.rs`). Aeneas SOURCE.flush + Flush.lean. TV `write_admit` 36/36 no pin 0172 (idle 8/8). Archive get trampoline em `db/lookup_archive.rs`. `db_rs_extracted=false`; `never_floor` inalterado. |
| Cluster SQL multi-write: Raft por range, 2PC entre ranges, grupo tamanho fixo | [RFC-0175](rfc/0175-sql-cluster-multiwrite-range-raft.md) | **draft — não aprovado; precisa de reviews extensos** (eixos produto/consenso/store/SQL/DST/IOPS/ledger/zero-downtime; ≥2 revisores ≠ autor; §Review). Conceito: queda de nós ≠ queda do SQL (multi-write); “sem líder” na mesma PK ≠ P0. Não autoriza implementação. L9/L29/L40 intactos. |
| Modelo matemático de escala (espectro + noisy neighbor) | [RFC-0176](rfc/0176-modelo-matematico-de-escala.md) | **done (P0–P2)** — `pedra scale-model --keys N --ram R` chama `scale_forecast` (1B @64 GiB P_best=5 bounded-cache; 10B P_best=6). Verus 10/0. Aeneas `Scale.lean`. P2.2 caixa oficial: 10M `mode=hot` / 25M `mode=bounded-cache` em `linux-gate-p149b`. Findings `findings/2026-09-07-rfc0176-spectrum/` e `findings/2026-09-07-rfc0176-p22-official/`. |
| overwrite_mc4 ≥1× vs Rocks default | [RFC-0180](rfc/0180-overwrite-mc4-gt1x.md) | **in-progress (P0.1–P0.38 done; P1.1 caixa todo)** — Darwin 3-run p42 mediana **1,002×** (named loss 0,816). Audit overfit → P0.34–P0.38 (`b944113`). Filhos: [0181](rfc/0181-leftover-follower-nao-pende.md) hang, [0182](rfc/0182-same-boot-write-path-harnesses.md) same-boot, [0183](rfc/0183-teto-apply-serial-e-1c.md) apply/1c. |
| Follower leftover não pende no último put | [RFC-0181](rfc/0181-leftover-follower-nao-pende.md) | **in-progress (P0.1 done)** — P0.31 resign; P0.36 drain-in-lead 0,87× reverted. Steal no resign. |
| Same-boot contra overfit do write path | [RFC-0182](rfc/0182-same-boot-write-path-harnesses.md) | **in-progress (P0.1 done)** — harnesses já existentes (YCSB a/f/apply/1c + Fjall absoluto + snapshot 1M). 3/3 quiet overwrite ≥1×. Fjall não é gate. |
| Teto apply serial e 1c overwrite | [RFC-0183](rfc/0183-teto-apply-serial-e-1c.md) | **in-progress (P0.1 done)** — apply_mc4 0,47× / 1c 0,82× / mc50 0,37× (Adaptive off). Medir mem-apply; despark 0055 P1.1 iff ≥15%, senão ceiling. Sem skiplist no P0. |
| Disk pressure: recusar writes, não corromper | [RFC-0179](rfc/0179-disk-pressure-fail-writes-not-corrupt.md) | **in-progress (P0+P1.4+P1.5 done)** — hard 64 MiB `DiskPressure` no `put`, PITR (`restore_pitr`/`ship_wal`/checkpoint/copy) e replica HA (`append_wal_bytes`/`catch_up` antes do pull). Reads de pé; probe `None` não recusa; `tracing::warn!` na transição. ENOSPC mid-write continua 0050. |
| Bounded-cache: store > RAM cap degrada pra lentidão (não Err/OOM) | [RFC-0173](rfc/0173-bounded-cache-ram-degrade.md) | **in-progress (P0+P1+P2.1 done)** — over-cap skip WARM + `DONTNEED` + first-keys paginadas do ficheiro (LRU 64 MiB); bloom cai se metadata > 512 MiB. Writes Ok. Residual P2.1: SoA ainda O(blocos) (~28 B/bloco). P2.2 cap 3/4 RAM aberto. |

| Verificação: **ENOSPC mid-flush volta a cercar (RFC-0050 P0.3)** — fuga no `main` público fechada nos dois entrypoints | [RFC-0050](rfc/0050-nine-axis-robustness.md) P0.3 | **done** — bulk MANIFEST/flush escapava sem fence em `Db::flush` e `ConcurrentDb::flush` (confirmado em HEAD c508c439 por stash + worktree + backtrace); `Db::flush` agora é choke-point (`flush_pipeline` + `fence_io_err`), ConcurrentDb cerca bulk/SST-write/install/WAL-rotate; 3 testes (1 existente + 2 novos) verdes; core 732/732, sim 61/61. `findings/2026-09-04-enospc-flush-fence-escape/` |
| Verificação: **clone freeze via a ver 41 mutantes AS-IS espelhados** (`*_as_is` invisíveis ao check por `SKIP_FN`) | classe F (clones) | **done** — `exec_fns_with_as_is` no `check_clones` + completude cross-pair; 41 mutantes registados no catálogo (92 fns / 45 ficheiros); teste negativo: drift injetado num twin → FAIL nomeia o par; glue `--all` verde. `findings/2026-09-04-clone-as-is-blindspot/` |
| Verificação: **noite REAL TCP em árvore limpa de HEAD c508c439** (RFC-0157 P2.3) — fecha o "pendente de árvore limpa" do r0-dirty | [RFC-0157](rfc/0157-capacidade-de-verificacao-multiplicadores.md) P2.3 | **done** — worktree limpa de HEAD; seeds frescas 0x15e41..0x15e48, 8/8 na 1ª tentativa, fator 1,08 (<2,0), kill targets distinct=3, eco de seed em todos os fingerprints; PCT d=3 (d=2 0/256, d=3 8/16384) e d=4 (35/16384, 2,39s) verdes com `1 passed` conferido. `findings/rfc0157-nightly/2026-09-04/` |
| Verificação: **noite r1 same-day (confirmação) em árvore limpa de HEAD c508c439** (RFC-0157 P2.3) — inclui a primeira onda FALHADA registrada por honestidade | [RFC-0157](rfc/0157-capacidade-de-verificacao-multiplicadores.md) P2.3 | **done** — wave 1 (0x16001..08): 8/8 mundos limpos mas fator 2,10 ≥2,0 (retry de flake documentado `left=0 napply=0` na 0x16005 dobrou o wall; console `2026-09-04-r1.wave1.console.txt` preservado); wave 2 (0x16101..08): `campaign: OK` 8/8 na 1ª tentativa, fator 1,08, distinct=3, PCT d=3 8/16384 e d=4 35/16384 verdes (`1 passed` conferido). `findings/rfc0157-nightly/2026-09-04-r1/` |
| Verificação: **noite r2 fechada na wave 3 (máquina quieta) + hardening do harness noturno** — ledger mecânico de seeds e gate de paralelismo por caminho crítico | [RFC-0157](rfc/0157-capacidade-de-verificacao-multiplicadores.md) P2.3 | **verde na wave 3** (seeds 0x16601..08): 8/8 na 1ª tentativa, `factor_norm=1,04` (wall 132s vs solo 117s), `distinct=3`, PCT d=3 8/16384 e d=4 35/16384 com `1 passed`. Waves 1-2 reprovadas e preservadas: w1 (0x16201..08) gate RAW antigo 2,08 (retry do flake, substância paralela); w2 (0x16401..08) reprovação LEGÍTIMA `factor_norm=2,98` (slowdown uniforme 3×, load ~60 externo). Hardening: `scripts/rfc0157_seed_ledger.py` (pre-flight "nunca reutilize seed" mecânico; recusa re-check da base queimada) + gate recalibrado `wall/max_i((i-1)*stagger+attempts_i*solo)` verificado nos runs do dia (1,00/1,02/1,00/0,99/1,04 PASS; 2,98/7,35 FAIL) + gate de máquina quieta com parser pt-BR consertado (`LC_ALL=C sysctl | tr -d '{}'`; v1 capturava `"{"` — uma espera de 45min saiu `exit 3` sem queimar seeds). `findings/rfc0157-nightly/2026-09-04-r2/` |
| Verificação: **REAL joint add-member em TCP de verdade (transição completa sob kill)** — R-joint ganha âncora REAL + modo de campanha add | [RFC-0119](rfc/0119-tcp-replica-joint-target.md) P2.3 | **done** — `cluster_real --add-member`: joiner n4 sobe pós-Put (catch-up é replicação), joint C-old,C-new via `client_add_member_joint`, C-new vivo nos 4 nós, Put sob quórum novo, SIGKILL do joiner (3-de-4 servem), restart serve chaves pré e pós-add. 7 bits `l28_tcp_add/cnew/svget/newget/jleft/caught/grown_ok` com twins Verus + 7 pares no catálogo (handlers: `tcp_node_disk_added_joint`, `tcp_node_disk_caught_up`, `tcp_node_disk_high_water`); teste REAL nomeado `l28_real_tcp_add_member_joint_cnew` (seed 0x0119_1E28, replay + `l28_durability_ok`) verde. Achados: catch-up do joiner é por **InstallSnapshot** (log pode ficar vazio — `caught` lê KV durável direto, agnóstico de transporte); parser `members=[1, 2, 3, 4]` era whitespace-token e via só `"[1,"` → `wait_member_gone` sempre-falso queimando ~110s/seed remove (solo 117s→5,6s; gates de fator seguem auto-relativos). Campanha: `RFC0157_MODE=add` no script (default remove byte-idêntico), smoke K=2 `campaign: OK` (0x1191e41/42, 10/10 gates, factor_norm 1,11, distinct=2); ledger 60→65 seeds bloqueadas. Noturna add: fechada no mesmo dia (linha r3 abaixo); campanha ≠ ∀ TCP. `findings/2026-09-04-rjoint-add/` |
| Verificação: **noite r3 — primeira noturna do modo ADD (K=8, dirty tree main)** + protocolo completo (gate máquina quieta, ledger, PCT) | [RFC-0119](rfc/0119-tcp-replica-joint-target.md) P2.3 / [RFC-0157](rfc/0157-capacidade-de-verificacao-multiplicadores.md) | **verde na 1ª wave** (seeds 0x16801..08, gate de máquina quieta esperou ~11min por sessão concorrente): 8/8 na 1ª tentativa, todos os bits add=1, `factor_norm=1,37` (solo 2s — efeito do fix do parser `members=[…]`; raw 6,50 mede quase só o stagger, o gate norm decide), `distinct=3`, PCT d=3 e d=4 com `test result: ok. 1 passed` (armadilha fail-open conferida). Ledger 65→73 seeds bloqueadas. Campanha ≠ ∀ TCP (R-joint segue); liveness não admitida (R-es). `findings/rfc0157-nightly/2026-09-04-r3/` |
| Verificação: **7 regressões vermelhas na árvore suja (lote de leitura em voo da sessão de edição) — delete ressuscitado pós-reopen cravado por sonda** | classe: regressão de lote concorrente (não do freeze) | **OPEN** — `--lib` 737 ok / 7 fail / 4 ignored; **os 7 passam no HEAD `c508c439`** (worktree limpa; `HEAD-RESULT.txt`). Destaque: `flush_preserves_delete_over_sst_put` + `get_mem_hit_skips_sst_and_still_sees_flushed` = **leitura silenciosamente errada** (valor apagado ressuscita após flush+reopen): disco íntegro (tombstone legível no `000002.sst` nível 0, `point_at=Ok(Some((2, Deleted)))`; MANIFEST e níveis certos), defeito no fast path novo `sorted_by_lo` (empate de los iguais seleciona a tabela velha; sonda: `RUN[0] tables=[1,0] disjoint_by_lo=false sorted_by_lo=true`); reproduz com `PEDRA_BULK=0`. Inclui `verified_report_matches_catalog` vermelho: os 7 pares `l28_tcp_*` (linha rjoint-add acima) entraram no catálogo sem a linha `on!`/gate no `verified.rs` — o contrato "claim/gate na mesma mudança" ficou incompleto. Exonerados por experimento de reversão + runs no HEAD: RFC-0151 P1.3 e o par `flush_publish`. `findings/2026-09-04-reopen-delete-resurrected/` |
| Verificação: **probe-order kernel — ordem newest-first dos candidatos a point-lookup é kernel verificado e wired** (RFC-0164 P0.1+P0.2) — fecha o dono formal do "delete ressuscitado" da linha OPEN acima | [RFC-0164](rfc/0164-probe-order-kernel-newest-first.md) P0.1+P0.2 | **done** — kernel puro `probe_order_kernel.rs` (átomo twinned `first_probe_on_equal_lo` + as-is que devolve a tabela velha; imagem empacotada zero-alloc `probe_order_covering`); Verus twin 4 verified / 0 errors (`scripts/verus_probe_order.sh`); planta DST `probe_order_on_live_equal_lo_is_not_ok` (put→flush→delete→flush ⇒ los iguais medidos). Wire P0.2 sobre o fix assentado da sessão concorrente: os dois `.rev()` de `db.rs` mortos, ramo não-disjunto de `lookup`/`lookup_sst_packed` itera `probe_order_covering`; sentinelas `flush_preserves_delete_over_sst_put` + `get_mem_hit_skips_sst_and_still_sees_flushed` verdes com o wire. Par 139º `probe_order` no catálogo com `wire=probe_order_covering` (novo campo: o lint só aceita se a fn estiver no `kernel_fn_allowlist` congelado do próprio kernel — sem enfraquecer os outros 138 pares); lint 867 ok / 0 fail; ci 1228 ok / 0 fail; freeze re-pinned (kernel_loc 12048 / handler_loc 95542). P1.2 destravado e fechado na sequência (linha abaixo); P1.1 (guard cache-frio == reopen) pendente. `findings/2026-09-04-reopen-delete-resurrected/` |
| Verificação: **arm de bisect single-candidate é kernel** (`run_pairwise_disjoint_los`) — a disjunção ESTRITA `hi[i-1] < lo[i]` que fecha o fix concorrente contra a variante fast-path do delete ressuscitado | [RFC-0164](rfc/0164-probe-order-kernel-newest-first.md) P1.2 | **done** — kernel no `probe_order_kernel.rs` (+as-is `<=`: arma o bisect no empate de los e, com sort estável, `by_lo[p-1]` cai na tabela VELHA — ressurreição pelo fast path); Verus 10 verified / 0 errors (modelo u64 no lugar de `[u8]`, padrão scan_guard); `SstRun::pairwise_disjoint` wired ao kernel (semântica idêntica ao fix deles, agora congelada); planta DST `run_disjoint_on_live_equal_lo_is_not_ok` (kernel=false, as-is=true, get vivo == None) + 4 testes de kernel; par 140º `run_disjoint`; lint 870 ok / 0 fail; freeze re-pinned (kernel_loc 12113 / handler_loc 95548). Vermelho `tx_fail_mid_commit_no_partial_and_seq_not_burned` (sim) atribuído por experimento ao lote concorrente (DurabilityFenced), não ao wire. `findings/2026-09-04-reopen-delete-resurrected/` |
| Verificação: **pass vacuo do point-cache guardado** — `get` vivo com cache frio == `get` pós-reopen no shape medido | [RFC-0164](rfc/0164-probe-order-kernel-newest-first.md) P1.1 | **done** — planta `cold_cache_live_get_agrees_with_reopen` (put→flush→delete→flush): primeiro `get` num handle fresco é frio por construção (`Db::get` devolve cache-hit sem tocar em mem/SST); guard exige frio==None, cacheado==None, reopen==None, frio==reopen — sob o walk as-is o vivo devolveria `Some("v")` com reopen `None`. P0+P1 do RFC-0164 fechados; resta P2.1 (twin Aeneas/Lean, fora de banda). `findings/2026-09-04-reopen-delete-resurrected/` |
| Verificação: **twin Aeneas/Lean do decision core probe-order** — terceira máquina sobre o `first_probe_on_equal_lo` extraído da produção (fechamento do RFC-0164: P0+P1+P2 todos done) | [RFC-0164](rfc/0164-probe-order-kernel-newest-first.md) P2.1 | **done** — crate `formal/aeneas/probe-order-kernel` (`[lib] path` = `probe_order_kernel.rs` de produção) + `scripts/aeneas_probe_order.sh`; stamp `SOURCE.probe_order` pinando o arquivo inteiro (drift em qualquer fn do kernel falha o ci). Toolchain pinado não traduz a superfície closure/iterator do kernel — 3 crashes medidos (trait `Iterator` core/iter/traits/iterator.rs:42; prepass core/slice/iter/macros.rs:153; core/iter/range.rs:980); reter `run_pairwise_disjoint_los` degrada a `axiom`+`sorry` e `<` de slice extrai como axiom. Extract = decision core `first_probe_on_equal_lo`(+as_is) transparente (0 axiom/0 sorry); `ProbeOrder.lean` (Lean 4.31.0): `first_probe_picks_newest`/`as_is_picks_oldest` (∀) + `distinct_tie_orders_disagree` (∀ empates distintos: spec ≠ mutante); lean build via `lean_vote.sh`; walk fns/regra disjunta continuam no Verus twin (10 verified) + plantas + lint wire. Nenhum tweak de produção para agradar o charon. `formal/aeneas/EXTRACT.md` |
| Verificação: **auditoria do harness do guest DERRUBA o reuso da v78 pela P2.2 — peer Rocks pinou 256 MiB×4, não default; md5s do slipstream pinados** (RFC-0153 P2.2 reopened; P2.1 refused segue) | [RFC-0153](rfc/0153-ram-scale-block-cache-bytes.md) P2.1+P2.2 | **reopened / refused** — montagem read-only do `preinject-fjall.bak` (backup do rootfs pré-inject fjall; VM viva intocada): md5s de engine em `/src/pedradb` batem com a v78 da campanha; o harness do guest é o **slipstream** (`/src/slipstream`, bench `snapshot_backends`), não o `scale.rs` do repo — e o `snapshot_rocksdb.rs` (mtime 30-Ago, pré-campanha) pinou `DATA_WRITE_BUFFER_BYTES=256 MiB`×4 no CF; `snapshot_pedradb.rs` base 256 MiB clampada a 64 pela campanha (`PEDRA_STAGE_MAX_BYTES=67108864`). Logo v78 = **Pedra@64 in-window vs Rocks@256×4** — o fechamento por reuso ("peer default 64") foi retraído; o >1× da rfc0162 segue válido e conservador (peer mais forte que default), mas a P2.2 estrita (peer default, janela 64) está **sem medição**; falta knob de janela no slipstream + campanha guest. Correções de rótulo: README rfc0162 ("Peer: RocksDB default" valia só p/ WriteOptions) e README 2026-09-02 (pino 256 do exemplo local nunca chegou aos CFs do Rocks — `open_cf_descriptors`); knob `HYDRATE_WRITE_BUFFER_BYTES` agora no nível de CF. A/B local ×3: Pedra@64 −23–29% vs Pedra@256 (**janela real é a célula local mais rápida** — P2.1 arena/skip-list refused); Rocks flat na janela; preview @64 Pedra 2,08–3,47× (DIAG, nunca oficial). **Prep da P2.2 no mesmo dia**: árvore `/src/slipstream` extraída do backup para o findings (byte-fiel, 6/6 md5s; harness que produziu os números oficiais agora reproduzível do repo) + knob `SLIPSTREAM_ROCKS_WRITE_PATH` no `snapshot_rocksdb.rs` (`default` = write path de fábrica; numérico = janela com cap ×4; unset = pino histórico 256 — syntax-check limpo, compilação fica pro guest) + runbook `guest-campaign-p22.md` com contrato de inject e gate novo de eficácia (`OPTIONS-*` do Rocks deve registrar `write_buffer_size=67108864` pós-run — default É 64 MiB). Md5s pinados no findings. `findings/2026-09-04-hydrate-flush-window/` |
| Bench: **célula estrita C1 da P2.2 medida no guest — Pedra@64 hidrata 1.347× (25M) / 1.596× (100M) vs Rocks factory; classe reconstruída/DIAG** | [RFC-0153](rfc/0153-ram-scale-block-cache-bytes.md) P2.2 | **c1-measured (reconstructed)** — janela tomada na pausa da série WINMISS (win8 ALL-DONE 23:59Z; swap-ep 04:33Z falhou no gate próprio `-ge 3`→`P22C1_NO_KNOB`, fix `-ge 2` + `python3 fdprobe` + echo `P22C1-DIRTY` aplicado via `fix_ep_cycle.sh` — portaria 502 no stop/start, fallback SIGTERM sem respawn, EP `9c4347f6` persistido, engine `20c3892d` intacto). Bateria 1 completa (`P22C1 ALL-DONE 06:16:28Z`), serial preservada pela outra sessão no instante do ALL-DONE (`guest-serial-p22c1-full.log` md5 `ed2f8968`): `harvest-p22.py` **GATES PASS** — smoke 2/2, 12/12 pernas rc=0, toda perna Rocks com `write_buffer_size=67108864`/`max_write_buffer_number=2` in-band, 0 BSTAGE. Medianas 3-run: 25M 28.8s vs 38.8s (**1.347×**, intra 1.32–1.37); 100M 118.1s vs 188.5s (**1.596×**, intra 1.60–1.76); settle Pedra 0.5/0.7s vs Rocks 2.9/4.4s. **Não oficial**: launcher reconstruído, árvore dirty do guest, `vm.dirty_ratio` inativo no container (20/10 registrado — vale para toda campanha prévia do guest), spread Rocks 100M 185–212s; não alimenta o floor de paridade. Bateria 2 (reboot da plataforma 07:00:15Z) morta no smoke pela tomada WINMISS9 (ALL-DONE 07:43:19Z) — zero impacto. Restore do EP superado pela cadeia win9 deles; arquivo de engine com knob **permaneceu** e rodou no win9 como pino histórico (gate lines `wbs=268435456`/`mwbn=4` na serial deles — comportamento byte-equivalente, higiene preservada). C2 (sweep 64×4) opcional, não rodada. `findings/2026-09-04-hydrate-flush-window/` |
| Bench: **C2 sweep (Rocks 64 MiB×4) medida — 1.320×/1.590×; conclusão da C1 robusta à contagem de buffers do peer** | [RFC-0153](rfc/0153-ram-scale-block-cache-bytes.md) P2.2 C2 | **c2-measured (reconstructed, DIAG)** — janela na mesma pausa win9 (serial deles preservada `1f045ec1`); `c2_cycle.sh` + `patch-p22.py swap-c2` (EP win9 salvo; engine `20c3892d` intacto), EP C2 `47165c4a` com assert duplo (wbs=67108864 **e** mwbn=4) e marcadores `P22C2-*`; bateria 08:57–09:22:56Z, serial preservada e puxada (`guest-serial-p22c2-full.log` `e2c6e062`); `harvest-p22.py c2` **GATES PASS** — 25M 27.8s vs 36.7s = **1.320×** (intra 1.24–1.48), 100M 118.6s vs 188.6s = **1.590×** (intra 1.56–1.61), toda perna Rocks `mwbn=4` in-band, 0 BSTAGE. Cruzado com C1: ×4 buffers ≈ +5% pro Rocks no 25M, flat no 100M; Pedra repetiu entre boots/épocas (28.8→27.8s, 118.1→118.6s, ≤3.5%) — Pedra@64 à frente nas duas células sob qualquer peer (1.32–1.35× / 1.59–1.60×). Mesma classe DIAG/reconstruída, mesmos caveats (dirty_ratio 20/10, um host, launcher próprio; não alimenta o floor de paridade). Restore do EP win9 em andamento (v1 perdeu a corrida de lock pro watchdog; v2 com loop nbd 0.05 s no kill; bateria 3 bootada pelo watchdog corrobora/replica antes do restore). `findings/2026-09-04-hydrate-flush-window/` |
| Verificação: **fault-grid completeness — o alfabeto de falhas × classes de op como grid 42-células decidido + bug de engine fechado** | [RFC-0165](rfc/0165-fault-grid-completeness.md) P0.1+P0.2+P0.3 | **done (P0)** — `FAULT_GRID` no `pedradb-sim` (7 `FaultKind` × 6 `OpClass` = 37 fireable + 5 vacuous com razão estrutural); teste nomeado `fault_grid_every_cell_is_fail_closed` exige `tripped()` (anti-vacuidade) + oráculo de consistência (prefixo acked sobrevive, unacked não ressuscita, reopen saudável abre) e fixa a política de superficiamento medida: Sync = **CommitUnknown** (bytes no cache, durability desconhecida — ambas visibilidades corretas), Remove = **advisory** (12 call sites `let _ =`; órfão é leak, MANIFEST é a verdade), Meta = arm permanente (probes `unwrap_or(0)` advisory; leituras fail-closed), `Interrupted`×Write/Meta = **std retria EINTR** (one-shot obrigatório — permanente é loop infinito, travado na 1ª iteração); gate `check_fault_grid` no lint parseia enums de `failing.rs` (variante sem célula = FAIL; sonda negativa `OpClass::Probe` → 7 FAIL nomeados); **bug achado pelo grid e corrigido**: cauda rasgada sub-header (1–6 bytes) pânicava o `WalReader` no reopen (slice invertido `7..1` no check de padding zero) — guard de bloco < `HEADER_SIZE` = EOF limpo + regressão `torn_sub_header_tail_after_records_is_clean_eof`; lint 874 ok / 0 fail (freeze LOC re-pinnado 12979/96408); residual `R-fault-grid` continuous (P1 media-lie, P2 eixo crash-points F×C + ConcurrentDb). `findings/2026-09-05-fault-grid/` |
| Verificação: **classe E — `c1_quorum` gradua da allowlist: o predicado de quórum de toda config ativa (joint: C-old **e** C-new) vira kernel ⊨ spec** | RFC-0166 P2.3 | **done** — board D–A vazio (cartoons none, freeze holes none); E = um par non-data_fate. `c1_quorum` era allowlist; agora tem `c1_quorum_as_is` (só C-old). Twin `verus/c1_modelo.rs` ganha exec `c1_quorum`/`c1_quorum_as_is` + spec as-is; `joint_add_witness` prova `!c1_quorum ∧ c1_quorum_as_is` no shape 2/3+1/4 — **6 verified / 0 errors**. Dente in-kernel `c1_quorum_on_joint_new_minority_is_not_ok`; planta Queued `c1_quorum_on_live_queued_is_not_ok`. Par `c1_cfg_quorum` (id distinto de `c1_quorum`=c1_holds no spec). Allowlist c1: só `joint_add_shape`. Catálogo **275**; lint **1487 ok / 0 gap / 0 fail**; freeze **62/16707/116791**. Testes raft `c1_modelo_kernel` **5/5**; planta Queued `c1_quorum_on_live_queued_is_not_ok` **ok** |
| Verificação: **classe D — plantas raft C1 pinam Queued RPC** (`c1_advance_commit` / `c1_modelo` deixam de ser cartoon in-kernel) | RFC-0166 P2.3 | **done** — o lint pedia `pin_dst_queued`/`RpcMode::Queued` no arquivo da planta; o dst_plant apontava para `c1_modelo_kernel.rs` (dente in-kernel, sem inbound). Duas plantas novas em `three_teeth_queued.rs`: `c1_advance_commit_on_live_queued_is_not_ok` (LiveQueued, joint-add C-old 2/3 C-new 1/4: FIXED commit_index=0 vs AS-IS 5) e `c1_modelo_on_live_queued_is_not_ok` (FIXED recusa serve vs AS-IS ack). Catálogo dst_plant apontado para as duas. Lint das plantas C1 Queued **verde** (as 2 falhas `must pin Queued RPC` saíram). `cargo test -p pedradb-store` **não rodou**: `pedradb-core` quebrado pela sessão concorrente (`CoreError::SnapshotTooOld` sem campo `cause` em db.rs — não extraí db.rs). Catalog 274; freeze pin 62/16696/116697 (handler_loc ainda deriva 4 LOC/ciclo da edição ativa deles). Pares meus recolocados no catálogo após wipe mid-slice |
| Verificação: **camada nova no programa — pedradb-cli enrolado: o switch `PEDRA_VERIFIED=1` (`verified_requested_from`) vira kernel ⊨ spec** (board D–E vazio; esta era a última camada sem kernel no programa de enrolar crates; cli escolhida porque o fn é o gate RFC-0058 P2.3 — só o valor exato `"1"` seleciona o perfil verificado / posix() sem ring; ignorar o flag abre o io_uring sob o banner verificado) | RFC-0058 P2.3 | **done** — `cli_kernel.rs` novo com `verified_requested_from(Option<&str>)` extraído de `verified_requested` em main.rs. Mutante AS-IS: `verified_requested_from_as_is` (**sempre false**: o flag não liga). Twin `verus/verified_flag.rs` close (tag u8: 0=unset, 1=`"1"`, 2=outro) com lemas `lemma_as_is_ignores_flag_one` e `lemma_unset_and_other_refuse` — **8 verified / 0 errors de primeira** (`scripts/verus_verified_flag.sh`). Dentes: `verified_requested_from_on_flag_one_is_not_ok` (FIXED true vs AS-IS false), rejects other values, agrees on unset. Catálogo +1 par (`cli_verified_flag` close, live_caller `verified_requested` main.rs) = **274**; enrollment explícito; corpus **85 pass / 0 fail**; lint **1481 ok / 0 gap / 2 fail** (plantas `c1_*` deles; `ae_f16_safe` graduou e saiu da allowlist); freeze re-pinado **62/16696/116598**. Testes: dentes **3/3**; integração `verified_flag` **2/2** (banner `no io_uring ring` / `verified_admits_ring=0` sob PEDRA_VERIFIED=1) |
| Verificação: **camada nova no programa — pedradb-dst enrolado: o fold `silent_wrong` da campanha vira kernel ⊨ spec** (board D–E vazio; resta cli; dst escolhida porque o fn é o veredito RFC-0020 P0.1 — conta trials que perderam a chave durável; sempre-zero abençoa silent loss e o DST mente) | RFC-0020 P0.1 | **done** — `dst_kernel.rs` novo com `silent_wrong_count` extraído de `run_silent_wrong_gate` (lib.rs) com nome exato. Mutante AS-IS: `silent_wrong_count_as_is` (**sempre 0**). Twin `verus/silent_wrong.rs` model (número de perdas `lost`: fixado reporta `lost`, as-is reporta 0) com lemas `lemma_as_is_blesses_lost_key` (∀ lost>0) e `lemma_zero_losses_agree` — **5 verified / 0 errors** (`scripts/verus_silent_wrong.sh`; o fold Vec/Seq recursivo não fechou no SMT — o modelo do veredito reportado vs sempre-zero é a decisão). Dentes: `silent_wrong_count_on_lost_seed_key_is_not_ok` ([true,false,true] → FIXED 1 vs AS-IS 0), all-ok e empty. Catálogo +1 par (`dst_silent_wrong` model, live_caller `run_silent_wrong_gate`) = **273**; enrollment explícito; corpus **84 pass / 0 fail**; lint **1476 ok / 0 gap / 2 fail** (plantas `c1_*` deles; `ae_f16_safe` novo no ae_ack_kernel da sessão concorrente baselined na allowlist); freeze re-pinado **61/16619/115421**. Testes: dentes **3/3**; gate vivo `rfc20_silent_wrong_gate_matrix` **ok** |
| Verificação: **camada nova no programa — pedradb-capi `unpack` enrolado: o discriminator de kind do handle C ABI vira kernel ⊨ spec** (board D–E vazio; restam cli/dst; capi escolhida porque `unpack` é o gate de type-confusion — um handle de DB apresentado como TX tem que recusar, senão `transaction_set` opera no objeto errado; `c_len` já era par, este é o furo U2 que restava) | U2 handle kind discriminator | **done** — `unpack` em `handles.rs` ganha mutante `unpack_as_is` (**ignora kind**: DB unpack como TX). Twin `verus/unpack.rs` model (u64 bits 63..62 = kind, 61..31 = gen) com lemas `lemma_as_is_unpacks_db_as_tx` (∀ raw com kind=DB e gen≠0: unpack(TX) recusa, as-is abençoa) e `lemma_null_never_unpacks` — **7 verified / 0 errors** (`scripts/verus_unpack.sh`; lição: SMT não computa `u64 >> 62` em testemunha concreta hex — o lema geral com `requires` nos bits fecha; exec com `>>` verifica contra a spec). Dente `unpack_on_kind_mismatch_is_not_ok` (pack KIND_DB, unpack KIND_TX: FIXED None vs AS-IS Some). Catálogo +1 par (`capi_unpack` model, live_caller `tx_handle` lib.rs) = **272**; `handles.rs` continua fora do glob `*_kernel.rs` (precedente `c_len` — não forcei kernel_paths para não explodir a surface de `Table`); corpus **83 pass / 0 fail**; lint **1471 ok / 0 gap / 2 fail** (plantas `c1_*` deles); freeze re-pinado **60/16465/114815**. Testes `cargo test -p pedradb-capi --lib handles -- --test-threads=1` **12/12** |
| Verificação: **camada nova no programa — pedradb-ops enrolado (57º kernel meu): o filtro da janela PITR (`pitr_record_in_window`) vira kernel ⊨ spec** (board D–E vazio; esta era a última camada de produto sem kernel — restam capi/cli/dst; ops escolhida porque o fn é o filtro de `restore_pitr`: um record WAL entra no restore iff `base < seq <= target`; sem o teto, writes depois do target aparecem no DB restaurado — PITR mente, o próprio teste do crate: seq 4 não pode aparecer em target 3) | PITR restore_pitr filter | **done** — `ops_kernel.rs` novo com `pitr_record_in_window` extraído da condição de `restore_pitr` (lib.rs) com nome exato. Mutante AS-IS: `pitr_record_in_window_as_is` (**só bound inferior** `ms > base` — futuro vaza). Twin `verus/pitr_window.rs` close (u64 = domínio real, espec em int) com lemas `lemma_as_is_leaks_future_seq` (∀ ms>target ∧ ms>base: fixado recusa, as-is abençoa), testemunha `lemma_seq4_at_target3_as_is_blesses`, `lemma_target_is_inclusive`, `lemma_base_is_exclusive` — **7 verified / 0 errors de primeira** (`scripts/verus_pitr_window.sh`). Dentes: `pitr_record_in_window_on_future_seq_is_not_ok` (4,1,3: FIXED false vs AS-IS true), `pitr_record_in_window_includes_target`, `pitr_record_in_window_excludes_base`. Catálogo +1 par (`ops_pitr_window` close, live_caller `restore_pitr`) = **271**; enrollment explícito; corpus Verus re-rodado: **82 pass / 0 fail**, stamp regravado; lint **1466 ok / 0 gap / 2 fail** (as 2 são as plantas raft `c1_*` da sessão concorrente, "must pin Queued RPC" — não toquei); freeze re-pinado **60/16465/114776**. Testes: dentes do kernel **3/3**; o teste vivo `pitr_restore_replays_shipped_wal_to_target_seq` **ok**. Suite completa 21 passed / 3 failed — as 3 falhas são o caminho **history-tier** (`pitr_restore_from_object_storage` timing do GC floor, `restore_history_round_trips_vlg_shaped_user_payload`, `restore_history_into_nonempty_dest_does_not_keep_leftover_sst_keys`), não o filtro PITR; não tocadas |
| Verificação: **camada nova no programa — pedradb-sql enrolado (56º kernel meu): o enquadramento da row key SQL (`row_key`, F71) vira kernel ⊨ spec** (board D–E vazio; resta ops +capi/cli/dst; sql escolhida porque o fn é a chave de INSERT/SELECT/DELETE — junção por barra `sql/row/{table}/{user}` faz `(a, b/c)` colidir com `(a/b, c)`, SELECT/DELETE bate na linha da tabela errada) | F71 | **done** — `sql_kernel.rs` novo com `row_key` extraído de lib.rs com nome exato; `table_marker`/`row_prefix` ficam `pub(crate)` no mesmo kernel (helpers do scan/DDL, allowlist de 2 fns). Mutante AS-IS: `row_key_as_is` (**junção por barra**). Twin `verus/sql_row.rs` model (prefixo `sql/r` 5 bytes + quadro u8-len de table + quadro u8-len de user) com lemas `lemma_slash_join_collides` (as-is colide os pares `(1, 2/3)` vs `(1/2, 3)`; framed diverge no byte de comprimento da table) e `lemma_equal_keys_frame_equal` — **9 verified / 0 errors de primeira** (`scripts/verus_sql_row.sh`). Dentes: `row_key_on_slash_join_collision_is_not_ok` (FIXED distinto vs AS-IS igual), `row_key_on_table_prefix_is_not_ok` (prefixo de tabela length-prefixed não vaza), `row_key_agrees_on_equal`. Catálogo +1 par (`sql_row_key` model, live_caller `exec_insert`) = **270**; enrollment explícito; corpus Verus re-rodado: **81 pass / 0 fail**, stamp regravado; lint **1460 ok / 0 gap / 2 fail** (as 2 falhas são plantas raft incompletas da sessão concorrente: `c1_advance_commit`/`c1_modelo` "must pin Queued RPC" — kernels `c1_modelo_kernel.rs`/`t1_modelo_kernel.rs` aterrissaram mid-slice, baselined no padrão: kernel_paths + allowlist + TCB_FREEZE_ALLOWLIST; não toquei as plantas deles); freeze re-pinado **59/16420/112853**. Testes do módulo **verdes**: `cargo test -p pedradb-sql -- --test-threads=1` **4/4** (3 dentes novos + 1 pré-existente) |
| Verificação: **camada nova no programa — pedradb-lease enrolado (55º kernel meu): o enquadramento do nome do lease (`lease_key`, F90) vira kernel ⊨ spec** (board D–E vazio; restam ops, sql +capi/cli/dst; lease escolhida porque o fn é a chave de TODA exclusão mútua — `try_acquire`/`try_transfer`/`try_release`/`holder`/`holders`: raw `lease/ || name` faz `lease/a` prefixo de `lease/ab`, scan meio-aberto vaza o irmão, dois holders no mesmo "lease") | F90 | **done** — `lease_kernel.rs` novo com `lease_key` extraído de lib.rs com nome exato (helper `push_len_pref` fica privado no kernel para não clonar o entry do index); callers vivos `try_acquire` (e os 4 irmãos) em lib.rs. Mutante AS-IS: `lease_key_as_is` (**prefixo cru + nome**, F90). Twin `verus/lease_key.rs` model (prefixo `lease/` 6 bytes + byte de comprimento u8 + nome ≤255) com lemas `lemma_framing_isolates_sibling_leases` (raw é prefixo, framed não — byte de comprimento na posição 6 diverge) e `lemma_equal_names_frame_equal` — **7 verified / 0 errors de primeira** (`scripts/verus_lease_key.sh`; reusa as lições do twin `len_pref`: invariante preserva prefixo e byte de comprimento, `Seq::new` pontual, `decreases`). Dentes: `lease_key_on_raw_sibling_prefix_is_not_ok` (FIXED não-prefixo vs AS-IS prefixo), `lease_key_distinct_names_are_distinct`, `lease_key_agrees_on_equal_names` — o teste pré-existente `lease_key_name_is_not_prefix_of_sibling_name` em lib.rs continua como planta viva (abre o store, adquire `a` e `ab`, range scan não vaza). Catálogo +1 par (`lease_key` model, live_caller `try_acquire`) = **264**; enrollment explícito; corpus Verus re-rodado: **78 pass / 0 fail** (lsm_r1 deles voltou verde nesta janela), stamp regravado; lint **1441 ok / 0 gap / 0 fail**; freeze re-pinado **56/15806/111731**. **Incidente de corrida**: a sessão concorrente regravou `catalog.json` no meio desta fatia e apagou os 4 pares meus (`oracle_diff`/`apply_majority`/`index_len_pref`/`lease_key`) — recolocados; kernels e twins em disco nunca foram tocados. Testes do módulo **verdes**: `cargo test -p pedradb-lease -- --test-threads=1` **11/11** (3 dentes novos + 8 pré-existentes, incl. o F90 vivo e o hotkey-cas) |
| Verificação: **camada nova no programa — pedradb-index enrolado (54º kernel meu): o enquadramento length-prefix (`push_len_pref`) vira kernel ⊨ spec** (board D–E vazio; restam ops, sql, lease +capi/cli/dst; index escolhida porque o fn é a decisão de enquadramento única por trás de `row_key` (F89) e `idx_key` (F65): sem comprimento explícito, scan de prefixo vaza irmãos e junção NUL colide pares distintos) | F89 / F65 | **done** — `index_kernel.rs` novo com `push_len_pref` (u32 BE \|\| parte) extraído de lib.rs com nome exato; callers vivos `row_key` e `idx_key` (lib.rs, 3 sites). Mutantes honestos: `push_len_pref_as_is` (**raw append**, F89: `a` vira prefixo de `ab`) e `push_len_pref_nul_as_is` (**junção 0x00**, F65: `(a, b\\0c)` colide com `(a\\0b, c)`). Twin `verus/len_pref.rs` model sobre `Seq<u8>` (byte de comprimento u8 modela o quadro u32 BE, partes ≤ 255) com specs `framed_spec`/`raw_spec`/`nul_spec` via `Seq::new` pontual + lemas `lemma_framed_injective` (mesmo quadro ⟹ mesma parte), `lemma_framing_isolates_siblings` (F89: raw é prefixo, framed não), `lemma_nul_join_collides` (F65: NUL colide, framed separa), `lemma_equal_parts_frame_equal` — **10 verified / 0 errors** (`scripts/verus_len_pref.sh`; lições: invariante do loop precisa preservar o byte de comprimento empurrado ANTES do loop (`r@[0]`); `Seq::new` pontual fecha onde concat `seq![len]+part` não fecha no SMT; `decreases` obrigatório nesta versão do Verus). Dentes: `push_len_pref_on_raw_sibling_prefix_is_not_ok` (FIXED não-prefixo vs AS-IS prefixo), `push_len_pref_on_nul_component_collision_is_not_ok` (FIXED distinto vs AS-IS igual), `push_len_pref_roundtrips_equal_parts`. Catálogo +1 par (`index_len_pref` model, live_caller `row_key` lib.rs) = **263**; enrollment explícito; corpus Verus re-rodado: **77 runners / 76 pass / 1 fail** — o fail é `lsm_r1` da sessão concorrente (regrediu mid-iteration: 62 verified / 2 asserts; stamp honesto fail=1); lint **1432 ok / 0 gap / 3 fail** (planta `lsm_reopen` deles + stamp deles; tudo meu verde); freeze re-pinado **55/15736/111226**. Testes do módulo **verdes**: `cargo test -p pedradb-index -- --test-threads=1` **8/8** (3 dentes novos + 5 pré-existentes) |
| Verificação: **camada nova no programa — pedradb-apply enrolado (53º kernel meu): o tamanho de quórum (`majority`) vira kernel ⊨ spec com o lema canônico de interseção de quóruns** (board D–E vazio; restam agora ops, sql, lease, index +capi/cli/dst; apply escolhida porque `majority` alimenta o gate `apply_committed_to_majority` que acka `propose_puts` — quórum pequeno demais acka commit que nenhuma interseção de maiorias protege) | P1.1 in-process majority commit (doc do crate, RFC-0010) | **done** — `apply_kernel.rs` novo com `majority(n) = n/2 + 1` (método `InProcessCluster::majority` agora delega com nome exato; caller vivo `apply_committed_to_majority` lib.rs:265, o gate do ack) + mutante honesto `majority_as_is` (**metade arredondada para baixo**: em cluster par, duas metades disjuntas são ambas "maioria" — sem interseção de quóruns, acks de metades opostas abençoam commits conflitantes, split-brain). Twin `verus/majority.rs` close (usize = domínio real, espec em `int`) com lema geral `lemma_quorums_intersect` (∀n≥1: 2·majority(n) > n — **qualquer par de quóruns compartilha um nó**), `lemma_as_is_never_intersects` (∀n≥1: 2·(n/2) ≤ n — o mutante NUNCA interseca), testemunha concreta do split-brain em n=4 e `lemma_single_node_quorum_is_one` — **7 verified / 0 errors de primeira** (`scripts/verus_majority.sh`; lição: `lemma_fundamental_div_mod` do vstd é broadcast — conecta div usize↔int sem chamada explícita no exec). Dentes: `majority_on_even_split_is_not_ok` (n=4: FIXED 3 com 2·3>4 vs AS-IS 2 com 2·2≤4), `majorities_always_intersect` (varredura n=1..=16), `majority_on_single_node_requires_that_node` (n=1: quórum é 1). Catálogo +1 par (`apply_majority` close, live_caller `apply_committed_to_majority` lib.rs) = **258**; enrollment explícito no `glue.kernel_paths`; corpus Verus re-rodado: **76 runners / 75 pass / 1 fail** — o fail é `verus_lsm_r1.sh` da sessão concorrente (RFC-0166 deles, aterrissou no MEIO desta fatia com 5× E0308: twin nem compila; stamp gravado honesto com fail=1 e o dente falha-fechado aponta para eles — correção é deles, quando passar re-run do corpus devolve o verde — **RESOLVIDO 15:05**: twin deles consertado, corpus re-rodado **76/76 pass / 0 fail**, stamp regravado fail=0; minhas 4 entradas baseline allowlist obsoletas graduaram e saíram (restam 8 fns não-classificadas deles no lsm_r1); lint **1415 ok / 0 gap / 1 fail** (única falha = esse stamp; tudo meu verde); freeze re-pinado **54/15645/110475** (drift de handler_loc 3× seguidas durante a edição ativa deles — assentou após). Testes do módulo **verdes**: `cargo test -p pedradb-apply -- --test-threads=1` **8/8** (3 dentes novos + 5 pré-existentes, incl. `in_process_cluster_majority_then_catch_up`) |
| Verificação: **camada nova no programa — pedradb-oracle enrolado (52º kernel): o veredito de paridade (`diff_snapshots`) vira kernel ⊨ spec** (board D–E vazio; "mais camadas" = enrolar camada sem kernel — restam ops, apply, sql, lease, index +capi/cli/dst; oracle escolhida porque o fn é a decisão de igualdade única por trás de `assert_pedra_matches_model` e `assert_pedra_matches_rocks`: se abençoa snapshots diferentes, toda paridade/DST plantada sobre ele mente) | P2.4 oracle harness (doc do crate) | **done** — `oracle_kernel.rs` novo com `diff_snapshots` (decisão única `left == right` → Ok/Err) extraído de lib.rs com nome exato; callers vivos `assert_pedra_matches_model` (lib.rs) e `assert_pedra_matches_rocks` (live.rs:74, via re-export — feature live-rocksdb). Mutante AS-IS: `diff_snapshots_as_is` (**só comprimento**: mesma contagem de entradas ⇒ Ok — valor errado silenciosamente passa na paridade). Twin `verus/oracle_diff.rs` model sobre `Seq<(u8,u8)>` (veredito em código 0/1) com specs `equal_snapshots`/`diff_snapshots_spec`/`as_is_spec` + lemas `lemma_equal_agrees`, `lemma_same_len_wrong_value_as_is_blesses`, testemunha concreta `as_is_blesses_wrong_value_witness`, `lemma_len_diff_both_refuse` — **9 verified / 0 errors** (`scripts/verus_oracle_diff.sh`; lições: spec referenciada em ensures de fn pub precisa `pub open`; testemunha de ∀-falso precisa asserts pontuais `a[0] != b[0]` para instantiar o trigger). Dentes: `diff_snapshots_on_wrong_value_is_not_ok` (mesma contagem/chave, valor difere: FIXED Err, AS-IS Ok), `diff_snapshots_on_extra_key_is_not_ok` (ambos recusam), `diff_snapshots_agrees_on_equal` (caminho verde). Catálogo +1 par (`oracle_diff` model, live_caller `assert_pedra_matches_model` lib.rs) = **257**; enrollment explícito no `glue.kernel_paths`; corpus Verus re-rodado: **74 pass / 0 fail**, stamp regravado 74/74; lint **1411 ok / 0 gap / 0 fail**; freeze re-pinado **53/15591/109844** (53º arquivo = `lsm_r1_kernel.rs` da sessão concorrente, RFC-0166 P2.2.1 deles, aterrissou sem twin/script nesta fatia — baselined no padrão estabelecido: kernel_paths + `kernel_fn_allowlist` 12 fns + `TCB_FREEZE_ALLOWLIST` com motivo; sai do allowlist quando registrarem kernel+twin). Testes do módulo **verdes**: `cargo test -p pedradb-oracle -- --test-threads=1` **5/5** (3 dentes novos + 2 pré-existentes) — camada independente do build quebrado do store (dep só de pedradb-core, live-rocksdb off) |
| Verificação: **camada nova no programa — pedradb-world enrolado (51º kernel): o veredito de trajetória das campanhas DST (`trajectory_violation`/`check_trajectory`, RFC-0059 P2.2) vira kernel ⊨ spec** (board D–E vazio; "mais camadas" = enrolar camada sem kernel; world escolhida porque o fn é o arbiter de `silent_wrong` e a camada já teve bug real de colapso de seeds) | RFC-0059 P2.2 / findings/2026-08-31-campaign-seed-collapse (motivação) | **done** — `world_kernel.rs` novo com `trajectory_violation` (cascata term→snapshot→applied sobre 3 coordenadas monótonas; caller vivo no `exchange` do run loop — todo `Trace::trajectory_violations` passa por ele) e `check_trajectory` (fold forense por (nó,range)) extraídos de lib.rs com nome exato; mutantes AS-IS: `trajectory_violation_as_is` (regra single-coordinate: só term — **janela de ressurreição**: watermark applied que regride é abençoado) e `check_trajectory_as_is` (fold composto com a regra AS-IS, forense mudo). Twin `verus/trajectory.rs` close sobre as 3 coordenadas (u64 = domínio real; códigos de veredito 0–3 em consts) com lemas `monotone_pair_agrees` + `as_is_blesses_applied_regression` + `as_is_blesses_snapshot_regression` — **9 verified / 0 errors de primeira** (`scripts/verus_world_trajectory.sh`). Dentes: `trajectory_violation_on_applied_regression_is_not_ok` (applied 7→6: FIXED `Some("applied_index")`, AS-IS `None`), `check_trajectory_on_resurrected_watermark_is_not_ok` (fold FIXED 1 linha vs AS-IS vazio), `monotone_trajectory_has_no_violation` (acordo no caminho verde). Catálogo +2 pares (`world_trajectory` close com live_caller `exchange` lib.rs; `world_trajectory_fold` atom twin=trajectory_violation, callers in-kernel — sem caller de produção, precedente `fields_kept`) = **256**; enrollment explícito no `glue.kernel_paths` (51 kernels / 514 pub fns — 507 classified); corpus Verus re-rodado pelo dente de stamp: **73 pass / 0 fail**, stamp regravado; lint **1405 ok / 0 gap / 0 fail**; freeze re-pinado 51/15033/109150. **Teste do módulo rodou 16:47**: `cargo test -p pedradb-world -- --test-threads=1` **75 passed / 2 failed**. Os 3 dentes do kernel estão verdes (`trajectory_violation_on_applied_regression_is_not_ok`, `check_trajectory_on_resurrected_watermark_is_not_ok`, `monotone_trajectory_has_no_violation`). As 2 falhas são `swarm::tests::world_membership_upgrade_7_nodes` ("windows did not exercise enough changes (0 < 7)") e `world_membership_upgrade_trajectory` ("mem=true: writes must land during windows") — testes de membership do swarm, não do kernel de trajetória; sintoma do mid-edit RFC-0167 (`snapshot_sent_at`) da sessão concorrente, não tocados |
| Verificação: **corpus Verus re-rodado inteiro + dente de stamp — 72/72 twins verdes sob o toolchain pinado; twin que nasce sem re-run do corpus agora é FAIL de lint** (fecho do furo R-verus "checker não pinado" do quadro de capacidade do RFC-0157 P2.1) | RFC-0157 P2.1 (re-run) / furo apontado pelo board `verificacao-next` | **done** — o registro anterior do corpus era de 2026-08-31 (57 twins); desde então **15 twins novos** entraram verdes em runner individual e nomeados no `--lint`, mas sem nenhum registro de corpus completo (`leveling`/`leveling_pick` commitados após aquele re-run; 9 runners ainda não-commitados da família cf/probe_order/write_ack/cqe; 4 de 2026-09-06 `d1_modelo`/`env_crash`/`spec_properties`/`wal_state` da sessão concorrente — todos PASS). Re-run `./scripts/formal/verus_check.sh --all`: **72 pass / 0 fail, exit 0** sob o pin `release/0.2026.08.23.fbbbbcf` (self-report `92f466f`, registrado desde 2026-08-30). Stamp `findings/rfc0157-verus-drift/last-all-run.json` (lista por-nome + pass/fail + pin + data — a reconstrução por-nome do conjunto ANTIGO só foi possível via `git log`, evidência de que registro só-contagem era insuficiente) + dente novo `check_verus_corpus_stamp` no `pedra_formal.py`: twin fora do stamp = FAIL fechado, runner obsoleto = FAIL, `fail>0` gravado = FAIL; **sonda negativa**: `verus_zz_probe.sh` falso → FAIL nomeando o twin, removido → verde. Nota de capacidade R-verus no `candidates.py` atualizada (checker/Z3 seguem TCB no never_floor — o stamp não é claim sobre o checker, é sobre a cadeia de evidência). Lint **1397 ok / 0 gap / 0 fail** após re-pin; freeze re-pinado 50/14862/108922 |
| Verificação: **montanha-fdb-recipes 100% classificado — children_kernel e fields_kernel zerados: as 7 fns baseline graduam; 11º crate sem allowlist e meu lado do `glue.kernel_fn_allowlist` chega a ZERO** (restam só 4 keys/7 fns da sessão concorrente: env_crash, wal_state, d1_modelo, write_ack) | RFC-0002 P23/P24 — F57 / F59 / F60 | **done** — children: `packed_children_start` (F57: start inclusivo `packed\|\|0x00`; as-is `to_vec()` deixa a chave-pai entrar na própria range de filhos) e `key_in_half_open` (F57/F59: meio-aberto `[start,end)`; as-is só `key>=start` deixa irmãos acima do bound vazarem) ganham mutantes + dentes `packed_children_start_on_raw_parent_is_not_ok` / `key_in_half_open_on_missing_end_is_not_ok`; `next_byte_in_packed_children` fecha twin close (u8 é o domínio real de produção; dente `theorem_next_byte_domain` varre os 256 bytes). fields: `field_kept` close (u64 real, dente `field_kept_not_nul_offset`); `child_bytes_after` / `decode_fields` / `decode_pair_first_nul` pares átomo twin=`field_kept` (convenção do próprio crate: Vec encode/decode é caller) com as-is `child_bytes_after_as_is` (rsplit trunca NUL dentro do id) e `decode_fields_as_is` novo (split em 0x00 cru + pad, always-Some; dente `decode_fields_on_raw_nul_split_is_not_ok` — zip `9\0 0` não round-tripa no length-prefixed). Twin `verus/children_range.rs` com execs `packed_children_start`/`_as_is` reescritos sobre `&[u8]`→`vstd::vec::Vec` (cópia com invariantes pontuais + extensionalidade de Seq; lição nova: `Seq::push` é **spec-only**, builder exec precisa `vstd::vec::Vec`) — **11 verified / 0 errors**; `verus/fields_nul.rs` **3/0** inalterado. Catálogo 247→254 (`children_start`/`children_half_open` model, `children_next_byte`/`fields_kept` close, `fields_suffix`/`fields_decode`/`fields_pair_nul` atom); live_callers reais `range_start`/`child_suffix`/`decode_pair_compat` (lib.rs:126/154/39-42); callers in-kernel nas 3 sem caller de produção (precedente `cqe_ring_refusal`). Lint **1396 ok / 0 gap / 0 fail**; freeze re-pinado 50/14862/108796. **Testes do módulo verdes** quando o build da sessão concorrente voltou (13:30): `cargo test -p montanha-fdb-recipes -- --test-threads=1` **36/36 lib** (inclui os dentes novos `packed_children_start_on_raw_parent_is_not_ok` / `key_in_half_open_on_missing_end_is_not_ok` / `decode_fields_on_raw_nul_split_is_not_ok`) + integração `children_model` 2/2, `fields_model` 4/4, `pack_model` 3/3 |
| Verificação: **pedradb-store 100% classificado — compact_kernel zerado: `compact_index_floor`, `compact_ready`, `peer_counts_for_compact` graduam; 10º crate sem allowlist** (allowlist 6→5 keys; meus restantes: só montanha 7 fns) | F27 / F28 | **done** — `peer_counts_for_compact` (F28: todo membro configurado conta no min(applied), mesmo particionado — peer offline congela o watermark) era wiring puro (twin já tinha exec+spec+lema `lemma_as_is_skips_offline`; live_caller `maybe_compact_logs` lib.rs:6853; dente `offline_peer_still_counts`). `compact_ready` (F27) e `compact_index_floor` (F27) ganham os mutantes que faltavam: `compact_ready_as_is` (pronto até em applied 0 — maquinaria de snapshot/compact dispara com log vazio/sem nada durável; dente `as_is_ready_at_zero_compacts_nothing`; live_caller `maybe_compact_logs` lib.rs:6873/6889) e `compact_index_floor_as_is` (floor fica em `through` — o catch-up AE re-requisita o índice recém-compactado, entrada ausente/tempestade de snapshot; dente `as_is_floor_re_requests_compacted_index` com a fronteira de saturação MAX igual nos dois; live_caller `compact_through` lib.rs:3252); twin `verus/compact_kernel.rs` ganha specs nomeadas + execs as-is + lemas `lemma_as_is_ready_at_zero` / `lemma_as_is_floor_re_requests_compacted` — **13 verified / 0 errors** (lição de novo: aritmética em spec é `int` não-delimitado, `(through+1) as u64` com guarda explícita de MAX). Catálogo 241→244; lint **1348 ok / 0 gap** — as 10 falhas do momento do pin são TODAS do `write_ack_kernel.rs` novo da sessão concorrente (50º kernel, mid-enrollment, mesmo padrão do d1_modelo); freeze re-pinado 50/14755/105217 (contém o estado mid-enrollment deles). **Teste do módulo compact_kernel fechado** quando o build voltou (2026-09-06 13:30): `cargo test -p pedradb-store --lib compact_kernel -- --test-threads=1` **9/9** |
| Verificação: **bloco store si/snapshot/index-val — 3 kernels zerados de uma vez: `point_get_prefer_applied`, `point_get_watermark`, `snapshot_needs_txn_meta_clear`, `value_len_tag`, `exact_value_children` graduam** (allowlist 8→6 keys; meus restantes: store compact 3 fns + montanha 7) | F84 / F42 / F40 / F78/F80 | **done** — fatia majoritariamente wiring (mutantes e dentes in-kernel já existiam): `point_get_prefer_applied` (F84) ganha os únicos additions de twin — specs + exec `point_get_prefer_applied`/`_as_is` + lema `lemma_prefer_applied_diverges` no `verus/si_reader.rs` (**9 verified/0**; lição re-confirmada: proof fn não chama exec — o lema diverge sobre specs, não sobre os execs); live_caller real `best_reader_for_key` (lib.rs:8913, caminho do `get` público). `point_get_watermark` e `value_len_tag` não têm caller de produção (política pura/átomo) — pin in-kernel `callers=[kernel]` como `cqe_ring_refusal`; twins já tinham os execs (wiring puro). `snapshot_needs_txn_meta_clear` (F40): wiring puro, live_caller real `on_install_snapshot` (lib.rs:6410). `exact_value_children` (F78/F80): par `twin_kind=atom` seguindo o precedente do próprio par `index_val` (entry `len_pref_value`, atom `value_len_tag` — o twin prova o átomo injetivo + modelo de bytes `0x00/0x01` do range filho; o encode Vec é caller), com 2 live_callers reais (`table_index_value_range` layers.rs:289 e `idx_children` fdb_layers.rs:336) e dente `as_is_leaks_nul_sibling` (`red\0foo` vaza no range cru). Catálogo 236→241; twins `si_reader` **9/0**, `snapshot_kernel` **4/0**, `index_val` **4/0**; módulos store seriais **si_kernel 7/7, snapshot_kernel 4/4, index_val_kernel 3/3**; lint **1334 ok / 0 gap / 0 fail**; freeze re-pinado 49/14543/104838 (drift de handler_loc é da sessão concorrente; nenhum src de produção editado nesta fatia) |
| Verificação: **pedradb-raft 100% classificado — ae_kernel e commit_kernel zerados: `ae_f16_safe` (gate F16) e `recover_last_applied` (re-apply F10) graduam; 9º crate sem allowlist** (repo 19→17 fns / 8 keys; sobram montanha 7, store 8 + 2 keys da sessão concorrente) | F16 / F10 | **done** — `ae_f16_safe` (predicado de segurança sem caller de produção além do teorema finito) ganha mutante `ae_f16_safe_as_is` (gate vacuamente true) e o twin `verus/ae_entry_action.rs` fecha o par: spec renomeada `ae_f16_safe_spec` (colisão com o nome do entry), exec `ae_f16_safe` com ensures == spec, lema testemunha `lemma_vacuous_gate_passes_committed_rewrite` — twin **6 verified/0**; dente in-kernel `ae_f16_safe_on_vacuous_gate_is_not_ok` + planta queued `ae_f16_safe_on_live_queued_is_not_ok` (entrada commitada em cluster 3-nós real + AE hostil de rewrite commitado: gate de produção rejeita o TruncateAndInstall sobre estado vivo, vacuo abençoa, o nó vivo recusa e não trunca o sufixo). `recover_last_applied` era wiring (twin `verus/commit_recover.rs` já tinha o exec com ensures == 0; caller vivo `RaftCluster::open` lib.rs:690 em live_callers): ganha spec `recover_last_applied_as_is` + lema `lemma_last_applied_as_is_strands` (**8 verified/0**) e planta queued `recover_last_applied_on_live_queued_is_not_ok` (put commitado via inbound no leader, kernel pin sobre range vivo, reopen completo do cluster no mesmo dir — dado commitado continua legível; as-is pularia a re-aplicação). Lições: lint exige planta raft no arquivo Queued (`three_teeth_queued.rs`) — dente in-kernel não basta; enum clone do store (`ae_ack_kernel::AeEntryAction`) ≠ enum do raft, planta importa `pedradb_raft::ae_kernel as raft_ae`. Catálogo 232→234; `cargo test -p pedradb-raft -- --test-threads=1` **110/110 (82 lib)**; módulo queued store **80/80 serial**; lint **1307 ok / 0 gap / 0 fail**; freeze re-pinado 49/14543/103693 (49º kernel = wal_state_kernel da sessão concorrente; d1_modelo_kernel deles enrolled no meio da fatia — as 7 falhas transitórias do lint eram essa corrida) |
| Verificação: **varredura dos crates de 1 fn — replicate, fold e stream zerados de uma vez; 6º/7º/8º crates sem allowlist** (repo 22→19 fns / 10 keys; sobram montanha 7, store 8, raft 2 + 2 keys da sessão concorrente) | F165 / F83 / F54 | **done** — `stamp_changed` (replicate) ganha exec no twin `verus/ship_guard.rs` (modelo fingerprint u64; **6 verified/0** com lema `rewritten_prefix_detected`) + dente novo `stamp_changed_on_rewrite_is_not_ok` (prefixo reescrito = rotação; as-is length-only envia 200 bytes desalinhados no mesmo shape); `isolated_child_byte` (fold) era wiring puro (twin já tinha exec+lema `as_is_leaks_sibling_b`, dente `theorem_next_byte_domain` nos 256 bytes); `next_seq` (stream) ganha mutante `next_seq_as_is` (wrap: após MAX acks ressuscita seq 0 — o residual de fronteira documentado vira dente) + lema `next_seq_boundary_no_wrap` (espec do as-is com `if` explícito: cast `as u64` em spec não fecha no SMT, mesma lição do leveling — modelar a decisão em vez do axioma de cast) — twin **6 verified/0**. Catálogo 229→232; testes `--test-threads=1` **replicate 22/22, fold 12/12, stream 13/13**; lint **1290 ok / 0 gap / 0 fail**; freeze re-pinado 48/14357/102986 |
| Verificação: **pedradb-dcs 100% classificado — apply_kernel e lease_kernel zerados: os 3 fns baseline graduam sobre twins e dentes já existentes; 5º crate sem allowlist** (repo 25→22 fns / 13 keys) | RFC-0002 P12 F12/F22 / P6 F7 | **done** — fatia de wiring puro (mutantes, dentes e twins já estavam verdes; faltava o par): `dcs_apply_should_advance` (bool núcleo do `_result`; o par existente só o tinha como `token_src`) ganha par próprio `dcs_advance_bool` no twin `verus/dcs_apply.rs` (3 verified/0); `lease_table_expired` → `lease_table` e `next_lease_id_after` → `lease_next_id` no twin `verus/lease_live.rs` (7 verified/0, lemas `unknown_as_is_is_immortal` e `as_is_reuse_can_collide` já provavam as divergências F7: id desconhecido imortal na as-is; contador reiniciado em 1 colide com id vivo em disco). `live_callers` reais: `lease_expired` e `from_open_db` (lib.rs:311/364); dentes in-kernel `as_is_freezes_on_cas` / `as_is_unknown_is_immortal` / `as_is_reuse_reanimates`. `cargo test -p pedradb-dcs -- --test-threads=1` **40/40 (34 lib), 0 fail**; lint **1281 ok / 0 gap / 0 fail**; freeze re-pinado 48/14321/102510 (kernel_loc intacto — nenhum kernel editado) |
| Verificação: **pedradb-journal 100% classificado — pin_kernel zerado: os 3 fns baseline graduam; 4º crate sem allowlist** (repo 28→25 fns / 15 keys) | Slipstream H1 / W4 / RFC-0024 (fold pin-after-apply) | **done** — `catch_up_pins_on_read` (canário W4), `fold_pins_on_read` (fold nunca pina na leitura), `next_pin` (monotônico) ganham mutantes `_as_is` no kernel (`catch_up` nunca pina → canário morto/re-leitura eterna; fold pina na leitura → perde entry entre leitura e apply; pin-on-receipt → **pin regride** passado o aplicado), twin estendido `verus/pin.rs` (**13 verified, 0 errors**, `scripts/verus_journal_pin.sh`) com specs nomeadas + lemas `fold_as_is_pins_on_read` / `catch_up_canary_as_is_never_pins` / `next_pin_as_is_regresses` (`m < pin ⇒ as-is == m < produção == pin`) e `empty_batch_keeps_pin` agora cobrindo os dois lados. 3 pares no catálogo com `live_callers` reais (`catch_up` lib.rs:35/37, guard de `peek` lib.rs:46) + 3 dentes com `JournalConsumer` vivo (pin não move em applied==pin; lote vazio = identidade; pin nunca regride). `cargo test -p pedradb-journal -- --test-threads=1` **12/12 lib + 2/2 int, 0 fail**; lint **1268 ok / 0 gap / 0 fail**; freeze re-pinado 48/14321/101448 |
| Verificação: **pedradb-io-uring 100% classificado — cqe_kernel zerado: os 4 fns baseline (F203/F208 + pin de recusa) graduam; o caminho `unsafe` Linux sai do residual** (allowlist do repo 31→28 net no instante do pin, com a sessão concorrente editando) | RFC-0074 / F203 / F208 / RFC-0156 P0.3 (R-uring) | **done** — `next_user_data` / `cqe_act` / `submit_complete_act` / `cqe_ring_model_admitted` ganham twin novo `verus/cqe_kernel.rs` (**11 verified, 0 errors** de primeira, `scripts/verus_cqe_kernel.sh`): lemas `f203_tags_never_zero` + `f203_distinct_states` (tags distintos de contadores distintos nunca colidem — leftover sempre distinguível), `f203_leftover_discarded` (CQE alheio nunca adotado), `eintr_harvest_diverges` + `f208_empty_cq_diverges` (submit Err: produção WaitMore/UseHarvested vs as-is ReturnSubmitErr que devolve o buffer com DMA pendente — shape UAF), `ring_refusal_diverges` (res-gate twin ≠ ring model, P2.2 segue refused). Mutante novo `cqe_act_as_is` (adota qualquer CQE) + dente `cqe_act_as_is_adopts_leftover`. 4 pares no catálogo com `live_callers` reais no `ring.rs` (`submit_sqe`, `harvest_ready_cqe`); par `cqe_ring_refusal` sem live_caller por construção (é o pin anti-claim, só o kernel o chama). `cargo test -p pedradb-io-uring -- --test-threads=1` **23/23**; lint **1253 ok / 0 gap / 0 fail**; freeze re-pinado ao vivo 48/14257/101451 (sessão concorrente enrolou 48º kernel). O ring em si (SQE layout, submit_and_wait) segue TCB — never_floor R-uring intacto |
| Verificação: **pedradb-http 100% classificado — cl_kernel zerado: os 3 fns baseline (F87/F88/F146) graduam e o crate inteiro sai do residual** (allowlist do repo 33→31 fns net: −3 meus, +1 env_crash da sessão concorrente) | RFC-0002 P32 (F87/F88/F146) / RFC-0150 P0 fn-surface tooth | **done** — `invalid_cl_as_zero` / `content_length_repeat_ok` / `short_body_vs_cl_is_error` (mutantes `_as_is` já no kernel) ganham twin no `verus/content_length.rs` (**8 verified, 0 errors**, `scripts/verus_content_length.sh`; novo bloco F146 + lema `lemma_f146_as_is_accepts_prefix`), 3 pares no catálogo (213) com `live_callers` = `read_req` (lib.rs:150/155/210) e **3 plantas live-HTTP novas** (`invalid_cl_as_zero_on_live_http_is_not_ok`: CL `abc` + corpo → 400 e GET 404, as-is 200-store vazio; `content_length_repeat_ok_on_live_http_is_not_ok`: CL `5`+`0` → 400 e GET 404, as-is last-wins grava vazio; `short_body_vs_cl_is_error_on_live_http_is_not_ok`: CL 5 com corpo de 2 bytes → 400 e GET 404, as-is 200-stores o prefixo). `cargo test -p pedradb-http -- --test-threads=1` **74/74 lib + 21 nos demais binários, 0 fail**; lint **1211 ok / 0 gap / 0 fail**; freeze re-pinado 47/13997/100732. Segundo crate do repo com allowlist zerado (após pedradb-core); sem touch em paridade Rocks |
| Verificação: **pedradb-core 100% classificado — allowlist do crate zerado: os 25 fns baseline dos 9 kernels graduam a par formal completo** (allowlist do repo 58→33 fns; nenhum key de pedradb-core resta) | RFC-0150 P0 (fn-surface tooth) / findings/2026-08-31-leveling-kernel-unenrolled | **done** — leveling 4 fns (`leveled_enabled`, `is_disjoint`, `runs_overlap`, `total_bytes`), scan_kernel 4, recover_kernel 4, compact_kernel 3, changelog_kernel 2, flush_kernel 1, manifest_kernel 1, probe_order_kernel 1, group_commit_kernel 5 ganham mutante `_as_is` no kernel + twin Verus + par no catálogo (25 pares; catálogo 179→204, hoje 208 c/ a family env_crash da sessão concorrente) + planta/in-kernel tooth. **8 twins verdes**: `verus/{leveling,scan_guard,sst_crc_fate,compact_decision,changelog_rebuild,wal_recover,group_commit,probe_order}.rs` — leveling 14 verified/0 errors após trocar a spec recursiva de slice-fold por modelo pairwise fuel-free (`total_bytes_spec(a,b)=a+b`; spec recursiva com `subrange` desestabilizava fuel/axioms do bloco inteiro, diagnosticado por bisect drop-one-block); lição registrada: teeth errados por raciocínio (disjoint `los=[1,6]/his=[0,5]`, não `[1,5]/[0,4]`) e triggers crus em `<` falham — predicado nomeado `adj_disjoint` resolve. Dentes notáveis: fsync-lie stacked (`stacked_liars`: 2 fsyncs mentidos admitidos como 1) e TCG; recovery `from_record_type`; probe newest-first; group_validate OCC; pct depth ceiling. Testes de módulo em série verdes: 98/98 (leveling 14, scan 13, recover 11, compact 13, changelog 11, flush 8, manifest 6, probe 11, group_commit 11). `live_callers` verificados nos pares de wire (db.rs `lookup`/`lookup_sst_packed`/`flush_pipeline`/`recover_ssts`/`prepare_*_compact`, concurrent.rs `validate_occ_batch`/`install_prepared_l0_off_lock`, wal/reader.rs, sst/table.rs, merge.rs). Freeze: 47 kernels / kernel_loc 13997 / handler_loc 100607 (sessão concorrente enrollou `env_crash_kernel.rs` e re-pinou). Lint **1194 ok / 0 gap / 1 fail** — a única falha é `pub fn of` do `env_crash_kernel.rs`, fatia ativa da sessão concorrente. Claims na forma kernel ⊨ spec relativo aos axioms; sem touch em paridade Rocks/published numbers |
| Verificação: **prova de fato — D1/R1/T1/C1 como refinamento machine-checked** (specs com dentes → Kani sobre produção → invariantes Inv-WAL/Inv-LSM → refinamento de handlers; CI passa a implicar prova) | [RFC-0166](rfc/0166-prova-de-fato-refinamento-propriedades.md) | **in-progress** — P0.1+P0.2 done: crate `pedradb-spec` com specs D1/R1/T1/C1 + dentes as-is provados (twin Verus 25 verified/0 errors 2×, 4 pares no catálogo); P0.3+P0.4 doing: harnesses Kani em `wal/recover_kernel.rs` (3 fns, domínios finitos exaustivos) + job CI `proof-check` (verus job verde no CI; kani job bloqueado por billing/spending limit do GitHub); P1.1 done: `env_crash_kernel.rs` (crash_legal = floor synced ≤ cut ≤ ceiling written; honest sync promove tudo via `fsync_promotes_pending` provado; `SyncPolicy::Lying` = Ok sem promover) + twin Verus 15 verified/0 errors 2× (`scripts/verus_env_crash.sh`) + planta sim `env_crash_on_live_recording_is_not_ok` (RecordingEnv honesto keep vs lying drop) + 6 pares no catálogo (env_crash/env_append/env_sync/env_barrier_floor/env_no_invented/env_honest_sync); P1.2 done: `wal/wal_state_kernel.rs` Inv-WAL (acked ⊆ synced ⊆ prefixo-recuperável) preservado por append/sync/ack/rotate + ponte `acked_survives_every_legal_crash` + twin Verus 18 verified/0 errors 2× (`scripts/verus_wal_state.sh`) + planta sim `wal_inv_on_live_recording_is_not_ok` + 6 pares (wal_state/wal_append/wal_sync/wal_ack/wal_rotate/wal_acked_survives); P1.3 done: `d1_modelo_kernel.rs` corolário nomeado **put Ok ⇒ sobrevive a todo prefixo de torn** (put_ok = append→sync honesto→ack-all; d1_modelo_theorem no twin; `put_lying_never_acks` suspende a premissa sob Lying) + twin 14 verified/0 errors 2× (`scripts/verus_d1_modelo.sh`) + planta sim `d1_modelo_on_live_recording_is_not_ok` + 2 pares (d1_put_ok/d1_modelo); P1.4 done: write_ack_kernel + twin 12/12 0 err (3×) + planta verified_write_ack_on_live_profile_is_not_ok; P2.1 done: lsm_r1_kernel 6/6 + twin verus/lsm_r1.rs 64/64 0 err (2×) + planta r1_modelo_on_live_delete_shape_is_not_ok + 4 pares (lsm_probe/lsm_compact/lsm_reopen/r1_modelo) + exec teeth; P2.2 done: t1_modelo_kernel 5/5 + twin verus/t1_modelo.rs 8/8 0 err (2×) + planta t1_modelo_on_live_abort_reopen_is_not_ok + 3 pares (tx_abort/tx_recover/t1_modelo); P2.3 done: c1_modelo_kernel 4/4 + twin verus/c1_modelo.rs 4/4 0 err (2×) + planta c1_modelo_on_live_joint_is_not_ok + 2 pares (c1_advance_commit/c1_modelo); P2.4 done: catalog object_kinds/campaign_prefixes (l28_*=campaign); lint check_proof_vs_campaign; profile_report D1/R1/T1/C1 spec+modelo; residuais R-fsync-lie=drive físico / R-glue cita exec-Verus; never_floor inalterado; P2.5 done: test_twin_mutation.py (rename entry + empty body ⇒ FAIL nomeado). P0.3 harnesses Kani shipped, Linux/docker blocked, CI kani waiting-on-billing; P0.4 workflow pinado, 1º run verde waiting-on GitHub spending limit |
| Verificação: **cf_kernel 100% classificado — os 6 fns restantes do allowlist graduam a par formal completo** (baseline 93→87; primeiro kernel do freeze sem nenhum fn baseline) | RFC-0150 P0 (fn-surface tooth, findings/2026-08-31-leveling-kernel-unenrolled) | **done** — `cf_family_of` / `cf_encode_effective` / `encode_cf_key` / `decode_cf_key` / `infer_sst_cf` / `compact_rewrites_sst_cf` ganham mutante `_as_is` no kernel, twin Verus model (spec + ensures + lemas de divergência as-is, 6/6 verified via `scripts/verus_*.sh`), par no catálogo (144 pares) e planta viva em `three_teeth_plants.rs` (14/14 verdes; ex.: tag gravado pelo engine == `infer_sst_cf` sobre bounds vivos; compact de lock seleciona exatamente os arquivos lock-tagged). Gate three-teeth generalizado para fn-head genérico (`decode_cf_key_as_is<'a,'b>` emprestado — exata-nome exigido, sem alocação no mutante); allowlist `cf_kernel.rs` zerado; lint 914 ok / 0 gap / 0 fail; fn surface 435 pub fns — 348 classified / 87 baseline; freeze re-pinnado 13014/96486. Dentes: family-loss (lock→default), raw-mode-loss, prefix-loss (colisão cross-CF), strip-loss (prefixo vaza no user key), tag-loss (misto→default), compact-leak (lock anda em default) |
| Verificação: **pedradb-http: path/form/auth 100% classificados — 19 fns graduam, baseline 77→58, o crate sai do topo do residual** (no http só cl_kernel resta, 3 fns) | RFC-0002 P31/P34/P35 / F76 / F79 / F85 / F88 / F91 / F92 / F101 / F145 / F149–F152 / F155 / F156 / F161 / F162 | **done** — 19 pares no catálogo (179) zeroam as allowlists dos 3 kernels. **7 twins close provando o corpo real** (`form_plus_byte`, `from_hex`, `plus_before_percent`, `query_u64_conflict`, `ascii_lower`, `ascii_upper`, `strip_authority_for_routing`) + 12 átomos com lema de divergência as-is (`x_pedra_is_fallback_only` F149, `default_port_equiv` F162, `scheme_prefix_folded_eq` F145, `authority_present` F161, `multi_values_can_conflict`/`bare_part_gate` F155/F162, `has_path_after_authority`, `split_userinfo`, `fragment_gate`, `ascii_eq_ignore_case`). Twins `verus/{bearer_scheme,form_plus,origin_path}.rs`: **37 verified, 0 errors**. 10 mutantes `_as_is` novos (shadow X-Pedra, hex-dígito-só, `HTTP://` exato, `userinfo@` cru …). Plantas: 2 live novas (`authorization_matches_on_live_http_is_not_ok` — Bearer errado + X-Pedra certo → 401; `host_authority_mismatch_on_live_http_is_not_ok` — absolute-form `http://evil.example` vs `Host: localhost` → 400, same-host 200) + `kv_http_method_case_insensitive` + 5 in-kernel discriminantes. `cargo test -p pedradb-http` **92/92 serial**; lint **1065 ok / 0 gap / 0 fail** (three teeth 177). |
| Verificação: **txn_kernel 100% classificado — os 10 fns do maior kernel baseline graduam a par formal completo** (baseline 87→77; twins **close** provando o corpo real, não modelo stand-in) | RFC-0002 P9–P10 / F47 / F34 / F52 / F35 / F36 / F49 / F50 | **done** — `revert_clears_status` / `revert_user_action` / `should_repair_si_hist` / `discard_cut` / `leftover_txn_is_aborted` / `next_txn_id_after` / `recover_si_generation` / `prepare_error_aborts_earlier` / `reserve_si_gen` / `unreserve_si_gen` fecham o twin close `verus/txn_kernel.rs` (11 exec fns com `ensures` + lemas de divergência as-is, verified `scripts/verus_txn_kernel.sh`; mutante novo `unreserve_si_gen_as_is` = rouba a reserva alheia). 10 pares no catálogo (156), 5 plantas **live em cluster real de 3 nós** (`three_teeth_queued.rs`, 78/78 em série): revert com fence abort visível via `get(txn_status_key)=="abort"`; preimage restaurado (não blind-delete); leftover prepared cercado no reopen; id de txn não reusado entre reopens (`commit_tx` ids 1→2); prepare falho aborta o range anterior e destrava a key. Plantas TX usam o harness Direct do próprio crate (propose síncrono; o pin Queued segue para os kernels RV/AE). allowlist `txn_kernel.rs` zerado (2º kernel 100%); lint 974 ok / 0 gap / 0 fail; fn surface 436 — 359 classified / 77 baseline; freeze re-pinnado 13021/96841 |
RFCs **0001–0012**, **0014**, **0015**, **0019** delivered. **0020** is the confidence/volume program; **0016/0017** remain robustness + cluster substrate (not Rocks field parity claims).

## RFC index (entry points)

[`rfc/`](rfc/) — start with
[0001](rfc/0001-pedradb-high-level-spec.md) (spec), engine
[0009](rfc/0009-rocksdb-class-engine.md), maturity wave 2
[0014](rfc/0014-rocks-pebble-redwood-maturity.md), audit
[0015](rfc/0015-audit-pedradb-correctness-fixes.md), production robustness
[0016](rfc/0016-pedradb-production-robustness.md), Montanha FDB-class
[0017](rfc/0017-montanha-fdb-class-substrate.md), products
[0010](rfc/0010-dbs-on-top.md), faults
[0011](rfc/0011-env-fault-injection.md), multi-node/wire
[0012](rfc/0012-next-significant-steps.md).

## Reproduce / advanced commands

```sh
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo bench -p pedradb-core --bench baseline
cargo run -p pedradb-cli -- demo /tmp/pedra-demo
# RFC-0169: dump de telemetria (latch off by default)
PEDRA_TELEMETRY=1 cargo run -p pedradb-cli -- stats /tmp/pedra-demo --telemetry
python3 scripts/check_no_ungated_prints.py
python3 scripts/telemetry_diff.py a/telemetry.json b/telemetry.json
cargo test -p pedradb-core --test telemetry_coverage -- --test-threads=1
cargo run --release -p pedradb-core --example telemetry_overhead_ab -- 2000
# Montanha TCP multi-host (RFC-0017 P0.1) — 3 processes, elect+put+majority
cargo test -p pedradb-store --test tcp_multihost
cargo run -p pedradb-store --bin montanha-tcp -- node --id 1 --data /tmp/m1 --bind 127.0.0.1:9701 \
  --peer 1=127.0.0.1:9701 --peer 2=127.0.0.1:9702 --peer 3=127.0.0.1:9703
# Real Linux lab on caixote (3-process local3; smoke on start + external ports)
./scripts/montanha_tcp_caixote.sh all
# Parallel World swarm campaign (RFC-0059): n_seeds start_seed workers n_nodes steps
#   env: PEDRA_SWARM_RANGES=4 (multi-range) | PEDRA_SWARM_DISK=1 (real-FS nodes)
#        PEDRA_SWARM_BUGGIFY=0 (faults off) | PEDRA_SWARM_CONSISTENCY=0 (checker off)
#        PEDRA_SWARM_LOG=dir (JSONL per seed + summary) | PEDRA_SWARM_DUMP=<seed>
#        (single-seed diagnostic: event trace; PEDRA_SWARM_TRACE=1 prints every event)
# exit 1 on any oracle failure (silent_wrong / row_half / cross-node consistency)
cargo run --release -p pedradb-world --bin world_swarm -- 16384 1 0 3 12
# docker smoke: docker run --rm --platform linux/amd64 -e CLUSTER=smoke ghcr.io/paulocsanz/montanha-tcp:p01-m3
# Multi-container mesh (experimental): MONTANHA_MESH=1 — blocked on caixote WG/hairpin today
# Local backup / PITR / migrate
cargo run -p pedradb-cli -- backup /tmp/pedra-demo /tmp/pedra-bak
cargo run -p pedradb-cli -- ship-wal /tmp/pedra-demo /tmp/pedra-bak   # after more durable writes (still in WAL)
cargo run -p pedradb-cli -- pitr /tmp/pedra-bak 1 <seq> /tmp/pedra-restored
cargo run -p pedradb-cli -- migrate /tmp/pedra-demo
# Linux: open with io_uring Env
# use pedradb_io_uring::open("/data/pedra")
# 3-node Raft (example ports)
cargo build -p pedradb-raft --bin pedra-raft-node
# cargo run -p pedradb-raft --bin pedra-raft-node -- --id 1 --data /tmp/r1 --bind 127.0.0.1:17001 --peer 1=... --peer 2=... --peer 3=...
```
